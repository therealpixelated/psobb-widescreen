## Appendix A — Cascade & per-draw detour deep-dives


### FUN_52da9930

**Role:** Stage A of the Ephinea widescreen runtime cascade = the master CONFIG/REGISTRY LOADER and cascade entry point. It is NOT itself a render-size or anchor calculator. It (1) opens the HKLM Ephinea-options key, reads ~55 DWORD config knobs via RegQueryValueEx, clamps each to range, writes them to ephinea.dll .data globals; (2) computes live displayW (_DAT_5318d210)/displayH (_DAT_5318dcdc) from registry resx/resy values, falling back to a 26-entry float resolution table at 0x53146240 when those are missing/equal/inverted; (3) applies the SSAA/supersample multiplier (_DAT_5387b938 = 2 or 4) to displayW/H, snapshots integer dims to _DAT_5388eca8/_DAT_53a9893c, and redirects four PsoBB.exe engine read sites (0x0082D140, 0x0082D18A, 0x008388F9, 0x00838905) to those snapshots; (4) opens the HKCU key for the windowed toggle (_DAT_5388fe08) and the co-op player struct at 0x53a90268 (count clamped <=10); (5) RegCloseKey both; (6) TAIL-CALLS Stage B (FUN_52dabbd0) at 0x52DAB9BB right after the final RegCloseKey. Returns a bitmask of registry-read error flags (local_14). Re-reads displayW/H fresh each call, so a config-reload re-invocation re-derives the whole widescreen pipeline.

**Formula:**
```
displayW = resx + roundfix(resx)   [reg val 0x5313f7bc, stored float _DAT_5318d210]
displayH = resy + roundfix(resy)   [reg val 0x5313f7d8, stored float _DAT_5318dcdc]
  roundfix(n) = *(double*)(&DAT_531761e0 + (n>>31)*-8)  // MSVC signed int->double correction (+0 if n>=0, +2^32 if n<0)

FALLBACK when (displayW==sentinel_0x53175df0 || displayH==sentinel || displayW<displayH):
  idx = clamp(reg[0x5313f7f8], <=0x19) -> _DAT_5388f988
  displayW = *(float*)(0x53146240 + idx*8)   // entry .W
  displayH = *(float*)(0x53146244 + idx*8)   // entry .H   (stride 8 = {float,float}, 26 entries)

SNAPSHOT + ENGINE REDIRECT:
  _DAT_5388eca8 = (int)displayW ; _DAT_53a9893c = (int)displayH
  *0x0082D140 = *0x008388F9 = &_DAT_5388eca8   // engine displayW read sites
  *0x0082D18A = *0x00838905 = &_DAT_53a9893c   // engine displayH read sites

SSAA APPLY (if _DAT_5387b938 != 0, factor 2 or 4):
  displayW *= _DAT_5387b938 ; displayH *= _DAT_5387b938
  _DAT_5318dccc = (int)displayW ; _DAT_5318decc = (int)displayH

SSAA factor select: aaIdx = clamp(reg[0x5313f324], 2..0x13) with {3->2, 6->5}; if aaIdx>4 a switch maps to _DAT_5387b938 (2|4) and/or _DAT_53884bd8 (MSAA 1..4) and resets _DAT_53a9895c per case.

TAIL-CALL Stage B: FUN_52dabbd0() consumes displayW/displayH -> gameRenderW=_DAT_5318deec, gameRenderH=_DAT_5318d124=480*S.

Return = local_14 bitmask (bit0=key open fail, bits1/2/3=specific value-read fails).
```

**Constants:** RESOLVED vs UNRESOLVED status of every constant Stage A touches:

VERIFIED FROM DECOMPILE (literal in code): HKLM/HKCU root 0x80000001, sam 0xf003f=KEY_READ; cbData 4 (DWORD reads), 0xa0 (two REG_SZ reads at val 0x5313fab8/0x5313fae0 -> FUN_52dd3f10 into 0x5382eb84/0x5382eb74), 0x24 (player struct). Clamp bounds all literal, e.g. SSAA idx 2..0x13 (3->2,6->5); HUD scale 0..4 (val 0x5313f26c); display mode 0..7; brightness 0x19..300 default 100 (val 0x5313f8e0); FOV/gamma 0..1000 (val 0x5313f8fc); player count <=10.

REFERENCED-ONLY / UNRESOLVABLE from the available static dump (Phase-1 warning CONFIRMED): every _DAT_5317xxxx/_DAT_5318xxxx .rdata scalar. I attempted resolution in ephinea_unpacked_base0x78480000.bin using file_va = liveVA-0x52d80000+0x78480000. THE LINEAR MAP IS INVALID FOR .data/.rdata: at file_va 0x78546240 (the supposed 0x53146240 res table) radare2 finds x86 CODE (movups/movss bytes 0f14e2/0f11...), not float pairs; the 0x531761e0 fixup region likewise decodes as code (8b4dec e8...). The unpacked bin is a proper PE (MZ, base 0x78480000, sections sect_0 .text 0x78481000, .banana packed 0x7b2c3000) whose section RVA layout != the live runtime layout the 0x52d8xxxx decompile addresses were captured at. The 1.5MB _ephinea_dump.bin covers only ~code, not 0x53146xxx/0x53175xxx .rdata. So NO resolution-table, breakpoint-ladder, or aspectMult values are confirmable from the artifacts.

TRUSTED (RPM-only, PATCHES_OVERVIEW.md:347 + stage_b_render_size.c, LOW confidence): Stage-B constants Stage A's output feeds: 0x53176128=640, 0x53176120=480, 0x53175e8c=4/3, 0x53175f20=2, 0x53175e64=1.0, 0x53175fc0=100; aspect ladder {1.30,1.45,1.60,1.70,1.85,2.00,2.35} -- all annotated 'exact RPM-only' (NOT verified from binary). The fixup &DAT_531761e0+(n>>0x1f)*-8 is the standard MSVC signed int->double correction (+0.0 for n>=0, +2^32 for n<0); structurally certain, byte-values unconfirmed.

Resolution fallback table 0x53146240: 26 entries x 8 bytes {float W, float H}, idx 0..0x19. Values UNRESOLVED.

**VAs touched (21):** PsoBB 0x0082D140 = ptr store &_DAT_5388eca8 (engine displayW read-site redirect), PsoBB 0x0082D18A = ptr store &_DAT_53a9893c (engine displayH read-site redirect), PsoBB 0x008388F9 = ptr store &_DAT_5388eca8 (2nd displayW read-site), PsoBB 0x00838905 = ptr store &_DAT_53a9893c (2nd displayH read-site), ephinea _DAT_5318d210 = displayW (float), ephinea _DAT_5318dcdc = displayH (float), ephinea _DAT_5388eca8 = int displayW snapshot, ephinea _DAT_53a9893c = int displayH snapshot, ephinea _DAT_5318dccc / _DAT_5318decc = int SSAA-scaled W/H, ephinea _DAT_5388f988 = resolution-table index (clamp <=0x19), ephinea _DAT_53a9895c = SSAA/AA quality (clamp 2..0x13, 3->2,6->5), ephinea _DAT_5387b938 = SSAA factor 2 or 4 (multiplies displayW/H), ephinea _DAT_53884bd8 = MSAA 1..4, ephinea _DAT_53870578 = render-path enable + _DAT_5382fe84/88/8c/90/94 flag group, ephinea _DAT_5388fe08 = windowed toggle (HKCU val 0x53140018 ^1), ephinea _DAT_53a9027c = co-op player count (<=10); _DAT_53a90268 = player struct base, ephinea _DAT_5388f9c0 = HUD scale 0..4; _DAT_5318e940 = display mode 0..7, ephinea _DAT_5382fe9c = HUD-scale percent 100..0x1c2 (consumed by Stage B), ephinea _DAT_53857a5c = widescreen-enable (consumed by Stage B); _DAT_5318d1e4 = brightness 0x19..300, ephinea READ .rdata (referenced-only, unconfirmable): res table 0x53146240/0x53146244, int tables 0x53143f60 & 0x53146308, neg-int fixup 0x531761e0, default 0x53175e64, 0x531760b4, 0x53175ff8, sentinel 0x53175df0, threshold 0x53176138, ~40 more non-display knobs (vsync 0x5318eed4, frame limit 0x5318f240, tex filter 0x5318dcfc, mipmap 0x53835c5c, fullscreen 0x5318eefc, gamma 0x5318d34c, audio/input/network) = non-visual, classified ui_misc/audio/coop_net/input

**Our impl:** DIFFERENT MECHANISM, equivalent goal. Our pso_widescreen.c (pixelateds-psobb-mods/psobb-patches/atomic/pso_widescreen-asi/pso_widescreen.c, 6459 lines) does NOT replicate Stage A's registry cascade:

1. CONFIG SOURCE: ours reads a FILE widescreen.cfg (Key=Value) in load_config() @ line 574, precedence INI Width/Height > widescreen.cfg > defaults (lines 894-950), driven by the launcher. Ephinea reads ~55 values from HKLM/HKCU registry. No registry path in ours.

2. DISPLAY DIMS / RES TABLE: ours computes render_w/render_h once in ws_compute_scale() @ lines 1042-1081 from cfg width/height (or logical_width/height in override_backbuffer mode, else A/C coord extents), then OVERWRITES the engine 6-entry resolution table at PsoBB 0x009006F4 with 6 copies of (W,H) (pso_widescreen.c:2058-2061; inline patch_res_table near line 4692). 0x009006F4 == Trinity resolution.h addrResolution[0]. Ephinea instead computes displayW/H from registry resx/resy (table 0x53146240 only as fallback) and REDIRECTS the four 0x0082D140/0x0082D18A/0x008388F9/0x00838905 read sites -- it does NOT write 0x009006F4. So we and Ephinea use DIFFERENT engine levers (table-write vs read-redirect) and DIFFERENT config sources (file vs registry).

3. SSAA: MISSING. Ephinea Stage A applies a 2x/4x supersample factor (_DAT_5387b938) to displayW/H. Our impl has no SSAA multiply in the display-size path (its integer-scale/override_backbuffer logical-canvas logic is a different feature -- downscaling a larger logical canvas, not render-at-NxN-then-resolve SSAA).

4. SCALE FACTOR: ours computes g_scale (A/B/C/D, hud_scale) in ws_compute_scale and applies via per-draw affine hooks + a static bake; Ephinea's S (HUD-scale/100 or 1.0) is computed in Stage B (FUN_52dabbd0), not Stage A. Our front-end is intentionally native (per project memory) and in-game uses the wide-affine path -- architecturally parallel to Ephinea Stage B/C, not Stage A.

**Port action:** Matching Stage A byte-for-byte would require porting the registry cascade, which is NOT recommended for our harness (we own config via widescreen.cfg/INI + launcher; the project deliberately moved off registry). Actionable faithful items:

1. (Optional, only for byte-exact parity) Single-boot idempotent bake that reads our resolved displayW/displayH (already g_scale.render_w/h / g_cfg.width/height), writes int snapshots, and -- to mirror Ephinea -- REDIRECTS the four PsoBB read sites 0x0082D140/0x0082D18A/0x008388F9/0x00838905 to a static int pair, INSTEAD OF (or alongside) overwriting 0x009006F4. First confirm on our build (live read/xref) which lever the engine HUD/anchor math actually consults -- Trinity uses 0x009006F4, Ephinea uses the redirect; both may work.

2. (Optional feature parity) Add SSAA: when an SSAA factor is configured, set the D3D backbuffer to displayW*N x displayH*N and resolve down. This is the only genuinely MISSING Stage A behavior. Low priority for widescreen LAYOUT correctness (SSAA is AA quality only).

3. RE-RUN SEMANTICS: ensure our scale/res bake re-applies on config reload (Ephinea proves Stage A->B re-runs via the tail-call; D3D-Device-Reset re-run is plausible-but-UNCONFIRMED per Phase-1). Our impl already re-bakes on front-end<->in-game flips (relayout_apply, per memory) -- the analogous idempotent re-apply; keep it value-guarded.

4. DOC FIX to carry forward: the resolution fallback table at 0x53146240 is 26 entries x {float W, float H} (stride 8), NOT 20 doubles as the clean port stage_a_config_load.c states. Correct that comment when the port is finished.

No new VISUAL lever is required from Stage A -- all visible widescreen geometry comes from Stage B/C and the per-draw detours (FUN_52da6e50/FUN_52dc4f60/FUN_52d8cb90/FUN_52db2240), separate functions. Stage A's only port-worthy job is the (displayW,displayH)->engine-read-site plumbing + optional SSAA.

**Open:** EXACT VALUES of the resolution fallback table (0x53146240, 26x{W,H} floats) and int tables 0x53143f60/0x53146308 are UNRESOLVED: the unpacked DLL .data/.rdata RVA layout != the live 0x52d8xxxx capture base, and file_va=liveVA-0x52d80000+0x78480000 lands in CODE not data. Needs a live ephinea.dll .rdata dump (0x53140000-0x53180000) or a correct RVA->paddr translation; project rule forbids live process, so unconfirmable from artifacts. | Aspect breakpoint ladder {1.30,1.45,1.60,1.70,1.85,2.00,2.35} and the aspectMult table are RPM-only (clean port literally annotates 'exact RPM-only'), never verified from binary -- same .rdata-dump blocker. | Whether our engine (PsoBB.IO build 59NL) reads display dims from the four redirect sites (Ephinea lever) OR the 0x009006F4 table (Trinity + our lever) OR both -- needs a one-time live read/xref on our client to decide which lever to port (static-only here). | Re-run on D3D Device Reset: PROVEN for config-reload (Stage A tail-calls Stage B fresh), but Phase-1 found NO IDirect3DDevice9::Reset hook, so a Device-Reset re-run is plausible-but-UNCONFIRMED. Affects whether our re-bake must also fire on Reset. | The two REG_SZ reads (vals 0x5313fab8/0x5313fae0 -> FUN_52dd3f10 -> 0x5382eb84/0x5382eb74, gated by 0x5382eb70) and the val-0x5313fe08 path (FUN_52dd3ec0 -> 0x5383d328) are unidentified config strings (likely custom-resolution or shader-profile names); not display-geometry-critical but purpose unconfirmed.


### FUN_52dabbd0 — Stage B (render-size compute + 7-level aspect-breakpoint ladder + title rune-emblem master-scale)

**Role:** widescreen_rendersize — Stage B of Ephinea's 3-stage runtime widescreen cascade (A=FUN_52da9930 config/res load → B=FUN_52dabbd0 THIS → C=FUN_52da7ff0 bulk anchor apply). Stage A tail-calls Stage B at 0x52DAB9BB right after RegCloseKey, having freshly loaded live displayW=_DAT_5318d210 / displayH=_DAT_5318dcdc from the registry (or res-table) with SSAA applied. Stage B's job: (1) aspect = displayW/displayH; (2) walk a 7-level nested-<= breakpoint ladder to select an aspect-multiplier bucket (writes scratch _DAT_5318d21c/_5318da7c/_5318f038=aspectMult + scale S=local_8); (3) derive S (=1.0 if widescreen OFF _DAT_53857a5c==0; =HUDScale/100 if ON and _DAT_5382fe9c!=0; else the bucket-selected scale); (4) write the engine render-size globals gameRenderW=_DAT_5318deec=(640*S*aspectMult)/(4/3) and gameRenderH=_DAT_5318d124=480*S; (5) write legacy scale _DAT_0097A910=dVar3/100; (6) call func_0x524b7c70 twice (x87 helper) → _DAT_5318efa4 & _DAT_5318ee38; (7) the load-bearing-for-us tail: multiply the TITLE-SCREEN RUNE/SEAL EMBLEM levers by S; (8) store master scale _DAT_5387b918=S. Output consumed by FUN_52dac110 (per-device VB anchor patch, guarded renderH!=480) and Stage C bulk loops (key off renderW-640 / renderH-480).

**Formula:**
```
aspect = displayW / displayH
S (master scale) = widescreenOFF ? 1.0
                 : (HUDScale!=0 ? HUDScale/100 : bucketScale)
aspectMult = ladder_select(aspect) over 7 ascending breakpoints; the sub-widescreen (<bp0) leaf returns 4/3 (identity)
gameRenderW = _DAT_5318deec = (640 * S * aspectMult) / (4/3)   [equivalently 480 * S * aspectMult]
gameRenderH = _DAT_5318d124 = 480 * S
_DAT_0097A910 = dVar3 / 100, where
   dVar3 = widescreenOFF ? (displayH/480)*K1*(_5318d1e4 + signfix(_5318d1e4))
                         : (displayH/(S*_53175fd0))*K1*(_5318d1e4 + signfix(_5318d1e4))   [K1=_53175ef8]
_DAT_5318efa4 = f(_531760bc * S);  _DAT_5318ee38 = f((_531760bc * gameRenderW)/640)   [f = func_0x524b7c70, x87 helper]
{0x0096E114, 0x0096E168..17C(6), 0x006F49FD, 0x006F4A57} *= S    (title rune/seal emblem)
_DAT_5387b918 = S
PARITY NOTE (PATCHES_OVERVIEW.md:96-98): the (640*S*aspectMult)/(4/3) form == the port's 480*S*aspectMult == 640*S*(aspect/(4/3)) ONLY under the live constants 640/480/(4/3); equality rests on RPM-only literals.
```

**Constants:** UNRESOLVED FROM STATIC ARTIFACTS (Phase-1 caveat confirmed). The _DAT_5317xxxx / _DAT_5318xxxx float/double pool that Stage B reads lives in ephinea.dll's runtime-unpacked .data/.rdata at 0x5317xxxx, which is NOT physically present in the unpacked dump. I verified this with radare2 on unpacked/ephinea_unpacked_base0x78480000.bin (PE32, baddr 0x78480000): mapping live 0x5317xxxx → file 0x7887xxxx lands inside sect_0 which is the CODE section (bytes at 0x78875e8c decode to x86 'd0 83 c4 0c 8b 46 40 85 c0...', not floats). Value-searched the whole 52MB file for the IEEE-754 doubles 640.0 (0000000000008440), 480.0 (..7e40), 4/3 (555555555555f53f), 100.0 (..5940) → ZERO hits. So the packer leaves these data pages uninitialized on disk; they are populated only at runtime. func_0x524b7c70 is likewise unmapped (0x77BB7C70 = 0xFF padding) → its identity (x87 floor/ceil/sqrt-class CRT helper, returns float10) is inferred, NOT byte-confirmed. The EPHINEA_CASCADE_RPM.md doc that holds the exact breakpoint ladder values (per PATCHES_OVERVIEW.md:416, cited as '1.30/1.45/.../5.25' and the aspectMult table) is NOT in the allowed artifact tree. Per RULES (no live process), I cannot RPM-confirm. STRUCTURALLY-VERIFIED literal map (from PATCHES_OVERVIEW.md:347-348, structure-verified / literals RPM-only): _DAT_53176128=640.0, _DAT_53176120=480.0, _DAT_53175e8c=4/3, _DAT_53175f20=2.0, _DAT_53175e64=1.0, _DAT_53175fc0=100.0. The 7 breakpoints are ascending and the <ee8 leaf's aspectMult=_53175e8c=4/3 (identity). All other bucket consts (_53175ea0, _53175ea4, _53176000, _53176044, _5317607c, ...) are referenced-only / unconfirmable. CONFIDENCE: structure HIGH (decompile + 2 ports + stock source agree); literal values LOW (RPM-only, marked unconfirmable).

**VAs touched (14):** READS: displayW=_DAT_5318d210 (registry/res W from Stage A), READS: displayH=_DAT_5318dcdc (registry/res H from Stage A), READS: widescreen-enable flag _DAT_53857a5c (0=off), READS: HUDScale percent _DAT_5382fe9c (0=use bucket scale), READS (breakpoints, doubles): _DAT_53175ee8 < _53175f00 < _53175f10 < _53175f18 < _53175f30 < _53175f40 < _53175f50 (7 ascending aspect breakpoints), READS (bucket consts): _DAT_53175e64(=1.0), _53175e78, _53175e8c(=4/3), _53175ea0, _53175ea4, _53175eb8, _53175f20(=2), _53175f58, _53175fa8, _53175fc8, _53175fd8, _53175ff0, _53176000, _53176044, _53176048, _53176064, _53176070, _5317607c, READS (base): _DAT_53176128(=640), _53176120(=480), _53175fc0(=100), _53175fd0, _53175ef8, _531760bc, _5318d1e4, &DAT_531761e0 (sign-fixup lookup table base, +0/-8 by sign bit), WRITES (scratch/bucket): _DAT_5318d21c, _DAT_5318da7c, _DAT_5318f038 (=aspectMult; also re-read for renderW), WRITES (RENDER SIZE — primary): _DAT_5318deec = gameRenderW; _DAT_5318d124 = gameRenderH, WRITES (legacy scale): _DAT_0097A910 = dVar3/100, WRITES (x87 helper results): _DAT_5318efa4, _DAT_5318ee38 (via func_0x524b7c70), WRITES (master scale): _DAT_5387b918 = S, WRITES (title rune/seal emblem *=S): _DAT_0096e114 (orb size), _DAT_0096e168/16c/170/174/178/17c (6-float orb-offset table), _DAT_006f49fd (seal-outer imm 430.0), _DAT_006f4a57 (seal-inner imm 178.0), DOWNSTREAM CONSUMERS (not written here): FUN_52dac110 reads _5318deec/_5318d124 (guard _5318d124!=_53176120) to patch D3D device-context VB regs; Stage C FUN_52da7ff0 keys bulk loops off (renderW-640)/(renderH-480)

**Our impl:** DIFFER (different architecture) + PARTIAL-MATCH on the rune-emblem tail. We do NOT run Ephinea's A→B→C runtime cascade. Our pso_widescreen.c (atomic/pso_widescreen-asi) uses an anzz1-style approach:
1) RENDER-SIZE EQUIVALENT — ws_compute_scale() (pso_widescreen.c:1046-1081): computes A=(AR/(4/3))*640*hud_scale (= our gameRenderW analog), C=480*hud_scale (gameRenderH analog), B/D companions, and render_w/render_h for the 6-entry engine res table at 0x009006F4. Same MATH SHAPE as Stage B's renderW=(640*S*mult)/(4/3) / renderH=480*S, but: (a) our 'mult' is implicit in game_aspect (g_cfg.game_aspect, baked 16/9), NOT selected from a 7-level breakpoint ladder — we have NO aspect-bucket ladder; (b) our S = hud_scale (front-end native 1.0; in-game 1.5), close to Ephinea's HUDScale/100 path but we never implement the widescreen-OFF legacy dVar3 / _0097A910 path; (c) we never write _5318xxxx (those are ephinea.dll-private; in PSOBB.IO the engine render-size lives at the anzz1 design_w/design_h 0x0098A4B8/0x0098A4B4 + the 0x009006F4 table, which we DO drive). So functionally MATCH-ish on renderW/renderH, MISSING the ladder + legacy-scale + _0097A910.
2) RUNE-EMBLEM TAIL — MATCH (independently derived). patch_rune_scale() (pso_widescreen.c:4613-4644) scales EXACTLY Stage B's 10-extra set by g_cfg.hud_scale: kRuneOrbSizeVA 0x0096E114, kRuneOrbOfsVA 0x0096E168 (loop i=0..5 → 0x0096E168/16C/170/174/178/17C), kRuneSealOutVA 0x006F49FD, kRuneSealInVA 0x006F4A57. Behavioral parity with Ephinea for this subset. Differences: it is a ONE-SHOT non-idempotent live read-multiply (static int applied guard; scale_float_inplace does nv=*addr*factor) gated behind INI PatchRuneScale, and it skips at hud_scale~=1.0 — whereas Ephinea re-applies *=S on every Stage-B run (config reload). It also does NOT touch _0097A910 / _5318efa4 / _5318ee38 / _5318d124 / _5318deec (those are ephinea.dll-internal render-size, irrelevant for PSOBB.IO).
RELEVANT SYMBOLS: ws_compute_scale, ws_scale_ctx{A,B,C,D,render_w,render_h,game_aspect,hud_scale}, g_cfg.game_aspect/hud_scale/enabled, patch_rune_scale, scale_float_inplace, kRune* macros, CC_DESIGNW_VA 0x0098A4B8 / CC_DESIGNH_VA 0x0098A4B4 (our render-size design dims).

**Port action:** For PSOBB.IO we do NOT need to port FUN_52dabbd0 literally (the _5318xxxx globals are ephinea.dll-private; our engine render-size lives at the anzz1 design_w/design_h + 0x009006F4 res table, already driven by ws_compute_scale). To MATCH Ephinea exactly where it is observable:
1) RUNE-EMBLEM (already matched, keep): patch_rune_scale already scales the identical 10-write set by the master scale. KEEP as-is BUT make it re-assertable: Stage B re-applies *=S on every config reload, so to match exactly, drive the rune-scale from the same single-boot-idempotent bake path used for design_w/h (capture the STOCK base values once, then write base*S each apply) instead of the current one-shot non-idempotent live read-multiply. That removes the 'applied' guard hazard and lets a re-assert worker re-run safely on front-end↔in-game flips (which we already do for relayout_apply). NET: convert scale_float_inplace(addr, factor) for the 4 rune levers to a captured-base * S write keyed on the live master scale.
2) ASPECT LADDER (optional, only if supporting many monitor aspects): Ephinea picks aspectMult from a 7-level breakpoint ladder; we hardcode game_aspect=16/9 then auto-detect from display AR (load_config aspect auto-detect). For our single-target widescreen this is adequate; a literal ladder port is BLOCKED on the RPM-only breakpoint/aspectMult values (not in static artifacts) — do NOT attempt until those are dumped live.
3) RE-RUN SEMANTICS: ensure the rune-emblem re-bake participates in the same config-reload / front-end↔in-game re-assert cascade as our other anchors (relayout_apply), since Ephinea re-derives ALL of Stage B's outputs from live displayW/H on every reload (PROVEN config-reload; D3D-Reset rerun plausible-but-UNCONFIRMED — do not assume it).
4) NO ACTION needed on _0097A910 / _5318efa4 / _5318ee38 / gameRenderW/H — ephinea.dll-internal, no PSOBB.IO analog.

**Open:** Exact breakpoint ladder values (7 ascending) and per-bucket aspectMult/scale literals are RPM-only — NOT in the allowed static artifacts (EPHINEA_CASCADE_RPM.md absent; data pool not in unpacked dump; live process forbidden). Confidence LOW on all _5317xxxx literals. | func_0x524b7c70 identity (sets _5318efa4 & _5318ee38): returns float10 from a float arg → inferred x87 CRT floor/ceil/round/sqrt-class helper, but 0x524b7c70 is unmapped in the unpacked dump (0xFF padding) so UNCONFIRMED. Its two outputs' downstream consumers also unmapped. | FUN_52dac110 (downstream VB-anchor consumer) first-8 branch subtracts _DAT_5317612c from gameRenderW (decompile :133529), NOT _DAT_53176128(=640); the clean port vb_anchor_patch.c assumes 640. _5317612c is 4 bytes past the 640 const — likely a separate float 640.0 vs the double, but unconfirmable. Possible port bug for the first-8 VB writes. | What are the scratch outputs _DAT_5318d21c and _DAT_5318da7c used for downstream? They are written per-bucket but their consumers were not traced in the allowed artifacts. | _DAT_0097A910 legacy-scale semantics: it is engine .data (0x0097Axxx, PSOBB-side address space), written by Ephinea but the formula uses the &DAT_531761e0 sign-fixup table (ephinea-internal). Whether PSOBB.IO needs any analog at 0x0097A910 is untested (we never write it). | The widescreen-OFF flag _DAT_53857a5c and HUDScale _DAT_5382fe9c are the LIVE/resolved values, distinct from Stage A's config-index versions _DAT_538785a4 / _DAT_5388f9c0 — the resolve step between them was not traced. | Stock psobb sets _DAT_00a96918=320.0 (line 655415) but our patch_rune_scale comment says center=(640,360); reconcile the emblem center under widescreen (320 vs 640) for exact title-emblem placement parity.


### FUN_52da7ff0

**Role:** Stage C of the Ephinea widescreen runtime cascade (live VA 0x52DA7FF0 in ephinea.dll, live base 0x52D80000). The REAL bulk anchor apply: it takes the render size already computed by Stage B (gameRenderW = _DAT_5318DEEC, gameRenderH = _DAT_5318D124) plus the master scale S = _DAT_5387B918, and STAMPS them into ~700+ engine UI-anchor floats in PsoBB.exe .data via six bulk loops (122/90/35/28/235/12 iters) and ~80 individual scalar geometry writes, THEN tail-calls the 92-VA function FUN_52da9280 at 0x52DA9062, and KEEPS writing ~25 more scalars + a 6-element column array AFTER it returns. It also sets the master window-resize scale 0x0098A320/0x0098A324, re-creates 12 sprite-table objects, runs a per-player split-screen UI rescale (when _DAT_53A9027C>2), does a localization/region pass that selects the string-table pointer written to 0x00841386 (=0x8EC39C default / 0x8F83A8 localized -- the SAME region/connect pointer the harness connect-fix note tracks), and re-arms the per-draw detour layer via FUN_52DC32E0(1). Re-runs on every config/resolution re-apply through the Stage A->B->C chain.

**Formula:**
```
Inputs (from Stage B): renderW = _DAT_5318DEEC = (640*S*aspectMult)/(4/3); renderH = _DAT_5318D124 = 480*S; S = _DAT_5387B918; displayW/H = _DAT_5318D210/_DAT_5318DCDC.
BULK LOOPS:
 L1 (122): *ptr = renderW
 L2 (90):  *ptr = renderH
 L3 (35):  *ptr += (renderW - 640) / 2          // center-X half-delta
 L4 (28):  *ptr += (renderW - 640)               // right-X full-delta (raw)
 L5 (235): *ptr += (renderH - 480)               // bottom-Y full-delta (raw)
 L6 (12):  *ptr += (renderH/2 - _DAT_531760E4)    // center-Y delta
 W-prop (2):  *ptr = round((*ptr/640) * renderW)
 H-prop (20): *ptr = round((*ptr/480) * renderH)
 ushort (12): *ptr = (ushort)((float)*ptr * _DAT_53175E04 * renderH)
SCALAR CLASSES:
 CENTER-X (Trinity MOD_X_C): newX = renderW/2 - K           (e.g. 0x00761CCC = renderW/2 - 210)  [note: decompile shows these via the 009d0040/00761ccc block as renderW/2 - constant]
 RIGHT-X  (Trinity MOD_X_R): newX = renderW - K             (e.g. 0x008F87D4 = renderW - 577_delta; 0x0096F2B8 = renderW - 400_delta)
 WELCOME block: newX = ((g_5318EE38 - Z)/Z + 1) * K   and   newX = ((g_5318EFA4 - Z)/Z + 1) * K   (Z = _DAT_531760BC sentinel)
 CONGRATS X: newX = renderW/2 - (K - oldX) * ((g_5318EFA4 - Z)/Z + 1)
 MASTERS: 0x008FB114 = renderW/2 ; 0x008FB110 = renderH/2 ; 0x008FB10C = oldVal / S
 FINAL right-edge: = renderW + _DAT_53175E44 ; bottom-edge = renderH + _DAT_53175E44 ; screen-safe = displayW/H + _DAT_531760B4
 COLUMN ARRAY (6): 0x009F4564+i*0x30 = i*_DAT_531760A4 + _DAT_53176074
 HUD CLAMP: 0x00A11464 = min(_DAT_53176140, _DAT_53176144 / S)
PER-PLAYER (numPlayers _DAT_53A9027C, p = numPlayers): if p>1: 0x00927318 = _DAT_53176150. if p>2: factor f=_DAT_53175E28; for i in 0..p-3: f = (i/_DAT_53176040 + 1)*f; f = (p-2)*f + 1; scale 5 entries @[0x5318E8D4] *= f; 0x0097F1BC += (p-2); 0x0090235C -= (p-2); 0x00902358 += (p-2).
```

**Constants:** RESOLVED structurally / by formula + Trinity literal comments (design-space stock value each anchor holds): _DAT_53176128=640.0 (STOCK_WIDTH; subtrahend in L3/L4), _DAT_53176120=480.0 (STOCK_HEIGHT; L5), _DAT_53175F20=2.0 (the /2 divisor for center anchors, L3 + congrats), _DAT_53175E64=1.0 (ONE in (x-Z)/Z+1 normalization), _DAT_5318DEEC=renderW (runtime), _DAT_5318D124=renderH (runtime), _DAT_5387B918=S (runtime). Trinity comment literals give stock values: Welcome W=577, Patch bars W=400, Now-loading X=368..624, F12 slide X=650, F9 chat W=624, congrats name X=256/384/128/514, congrats Y=39/319, seal=320/240, Press-Enter X=192, Start menu X=210, Phantasy/Star wordmark 64/320/61/317/576.

NOT RESOLVABLE FROM STATIC ARTIFACTS (confidence=low): the literal float values of the _DAT_5317xxxx .rdata constants (_DAT_531760E4 [L6 center-Y offset], _DAT_53175E44 [HUD-edge pad, ~0], _DAT_531760B4 [screen-safe pad], _DAT_531760A4 + _DAT_53176074 [column-array step+base], _DAT_53176088 [final inc], _DAT_53176140/53176144 [HUD clamp pair], _DAT_53175E04 [ushort H-scale factor], _DAT_53176190 [HUD-edge const], _DAT_53176110, _DAT_53176150 [2P value], _DAT_53176040 [per-player divisor], _DAT_53175E28 [per-player seed], _DAT_531761E0 [int->double bias table]). REASON: the unpacked DLL (llama-bob-ephinea-re/unpacked/ephinea_unpacked_base0x78480000.bin) is a post-.banana-unpack memory image whose .rdata/.data are NOT at standard PE RVAs -- VA-arithmetic (liveVA-0x52D80000+0x78480000, and the PE section-paddr mapping) both yield garbage floats when probed (640/480/2 do not appear at their expected slots). r2 disasm at the computed file VA 0x784A7FF0 shows unrelated SIMD code, confirming the code layout also diverges. These are the same constants PATCHES_OVERVIEW.md flags as 'referenced-only / undumped .rdata -- structure verified, literals RPM-only'. They would need a LIVE read_memory of ephinea.dll (disallowed for this task) to pin exactly.

**VAs touched (12):** LOOP BASES (ptr-tables, each entry points to a PsoBB float): 0x5318EEDC(6 obj), 0x5318F264(6 obj), 0x5318EE3C(12 aspectMult), 0x5318F040(122 =renderW), 0x5318E6E0(90 =renderH), 0x5318E650(35 +=(W-640)/2), 0x5318F2A0(28 +=(W-640)), 0x5318E9C8(235 +=(H-480)), 0x5318EF00(12 +=(H/2-off)), 0x5318F00C(2 W-prop round), 0x5318EF40(20 H-prop round), 0x5318E8FC(12 ushort*H), 0x5318E8D4(5 per-player), READ runtime globals (Stage A/B written): 0x5318DEEC=gameRenderW, 0x5318D124=gameRenderH, 0x5318D210=displayW, 0x5318DCDC=displayH, 0x5387B918=S/HudScale, 0x5318F038=aspectMult, 0x5318D21C/0x5318DA7C=bucket scaleX/Y, 0x5318EE38/0x5318EFA4=Stage-B intermed, 0x5318E8EC=per-player factor, 0x53A9027C=numPlayers, 0x5388ED90=languageId, SCALAR WRITES (Trinity-named): 0x006F4CF2/CFA(Phantasy start X/Y),0x006F4D02/D0A(Phantasy end X/Y),0x006F4D4E/D56/D5E/D66(Star BB start/end X/Y),0x009D0040(Press-Enter X),0x00761CCC/CDE(Start menu X),0x0076236C(Register UID menu X),0x007625F8(UID pw X),0x007627CF(UID user X),0x006F4922/4936(Seal X/Y),0x008F87D4(Welcome W),0x008F87E4(Press-any-key W),0x0096F2B8(Patch bars W),0x007584EE/0x0075851F(F12 name/time slide X),0x00978454/0x00978458(Lobby name/time-in-menu X),0x009712EC(F12 bg bottom Y),0x007165B8(F9 chat W), PATCH-STATUS bars 0x009F0ADC/AF4/B0C/B24/B3C/B5C, 0x0070D350/D2F6(title-bg center W), NOW-LOADING 0x009FF080/098/0B0/0C8/0E0/0F8/110/128/1A8, CONGRATS 0x009CA53C/544/54C/554(name X), 0x0093B8AC/A8(name Y), MASTERS 0x0098A320/0x0098A324(WindowResizeScaleX/Y), 0x008FB114(renderW/2),0x008FB110(renderH/2),0x008FB10C(/=S), 0x00A11464(HUD clamp), PER-PLAYER 0x00927318,0x0097F1BC,0x0090235C,0x00902358, FINAL block 0x0077F2D1/2ED/30D/32C/362/386/3A7/3B2/3C7/3D2, 0x0091FF30/2C/00/FC, 0x00973138/134/130/12C, 0x009F4564/594/5C4/5F4/624/654, 0x009F4654, 0x0096FFDC, SPRITE-ATLAS table 0x009A3840..009A38D8 (24 entries), COLOR 0x009B8DB4/B8/BC/C0/E8/EC (0xFF181818/0xFA181818), NON-WS branch 0x00841386 = 0x8EC39C/0x8F83A8 (region/string-table ptr)

**Our impl:** DIFFERENT ARCHITECTURE (mostly MISSING-by-design + a few overlapping targeted patches). Our pso_widescreen.c (C:/Users/u03a9/Repositories/pixelateds-psobb-mods/psobb-patches/atomic/pso_widescreen-asi/pso_widescreen.c, 6459 lines) does NOT replicate Stage C's 6 bulk loops or its 80-scalar cascade. Instead it uses the anzz1 GLOBAL-AFFINE model: a single 2D affine 0x00ACC0E8/EC (= render_w/design_w) plus a master design_w 0x0098A4B8 (=853.333*hud_scale) / design_h 0x0098A4B4, so most of what Stage C bakes per-anchor is handled implicitly by the affine at draw time. Globals: g_cfg.hud_scale (master, default 1.0), ws_compute_scale(), apply_startup_bakes() (boot-time bakes), relayout_apply() (front-end<->in-game re-bake), ingame_scale_pin (worker-thread driven), the deanchor asm hook around 0x00ACC0E8, pb_fix_bar (RenderUIQuad hook). OVERLAP: we DO touch a handful of the SAME anchor VAs Stage C writes, but as isolated targeted patches not the systematic cascade: 0x009A3840 HUD-rect/atlas table (rewritten for the title wordmark), 0x006F4CF2..0x006F4D66 (8 title-wordmark imm32 in title_real_mode), 0x009FF080..1A8 (now-loading floats), 0x0096F2B8 (patch-bar W via PER-FRAME RMW, intentional, vs Ephinea's single bake). We do NOT set Ephinea's window-resize scale 0x0098A320/0x0098A324, do NOT touch the 122/90/235/etc loop tables, do NOT do the per-player split-screen rescale, the congrats-screen, F12-slide, welcome-msg, or the 0x00841386 region branch. Our master is 0x0098A4B8 (anzz1's design_w), a DIFFERENT global from Ephinea's per-anchor .data. So for any element NOT covered by our global affine, Stage C is effectively MISSING in our port.

**Port action:** DECISION REQUIRED, not a blind port. Two coherent paths: (A) STAY on the anzz1 global-affine model (our current architecture) and only port the SPECIFIC Stage-C anchors that the affine does NOT correctly place (the known residual gaps: lobby NN-NN bar, quick-chat float, F12 overlays, now-loading, congrats) as targeted idempotent bakes; do NOT import the 6 bulk loops. (B) Faithfully replicate Stage C: this is the heavier 'be Ephinea byte-for-byte' route. If (B): implement as a SINGLE-BOOT IDEMPOTENT bake that (1) computes renderW = (640*S*aspectMult)/(4/3) and renderH = 480*S exactly as Stage B, (2) runs the 6 loops with the EXACT counts 122/90/35/28/235/12 over the SAME PsoBB.exe pointer-tables (these tables live in PsoBB.exe .data, so the target floats are directly addressable from an ASI -- enumerate them from a live ephinea diff or from the loop-base ptr-tables), (3) replays the ~80 scalars in the SAME ORDER (order matters: many use += so a double-run shifts twice -- our bake MUST guard against re-entry, e.g. write absolute values not += , or snapshot stock once), (4) re-runs on config-reload AND should be re-asserted on the front-end<->in-game flip (our relayout_apply already does this for our model -- extend it), since Ephinea re-runs via Stage A's tail-call chain. CRITICAL idempotency note: L3/L4/L5/L6 and the final-block use += on the LIVE value, so Ephinea relies on re-deriving from a fresh stock base each cascade run; a naive ASI re-bake must cache the stock float per VA and recompute from stock, never accumulate. Before either path: resolve the _DAT_5317xxxx literals via a LIVE read_memory of ephinea.dll (the only way -- static unpacked DLL won't give them). Do NOT touch the 0x00841386 region branch (non-widescreen; it is the localization/connect string-table selector the harness connect-fix already manages -- importing it would fight pso_connect_fix_dyn.ps1).

**Open:** Exact float values of the _DAT_5317xxxx .rdata constants (531760E4 center-Y offset, 53175E44 HUD pad, 531760B4 screen-safe pad, 531760A4/53176074 column step+base, 53176088 final inc, 53176140/53176144 HUD-clamp pair, 53175E04 ushort factor, 53176190, 53176150 2P-value, 53176040/53175E28 per-player) -- UNRESOLVED from the unpacked DLL (packer relocated .rdata; VA math + PE-paddr both yield garbage). Need a live read_memory of ephinea.dll (out of scope here). | The exact membership of each bulk-loop pointer-table (which 122/90/35/28/235/12 PsoBB.exe floats) -- the decompile only shows the table BASE in ephinea.dll .data; resolving the actual PsoBB target VAs needs the live table contents (read_memory of [0x5318F040] etc.) or an ephinea_patches A/B diff. | func_0x524b7cb0 (used in W-prop/H-prop loops) and func_0x524b7c70 (Stage B) -- assumed round/trunc; not disassembled here. Confirms whether the proportional loops round-to-int or keep float. | Whether Stage C re-runs on D3D Device Reset (vs only config-reload). PATCHES_OVERVIEW marks config-reload PROVEN but Device-Reset UNCONFIRMED (no visible Reset hook in Phase 1) -- affects whether our re-bake must also hook IDirect3DDevice9::Reset. | Does our global-affine model actually leave any of Stage C's scalar anchors visibly wrong at hud_scale=1.0 16:9? Several Stage-C targets (start-screen wordmark, register-UID, welcome) are FRONT-END which we render native -- so Ephinea's Stage-C writes there may be intentionally NOT needed in our model. Needs a live A/B (ephinea_patches) to separate 'missing & visible' from 'missing & irrelevant'. | Per-player (split-screen) block: PSOBB Blue Burst has no local split-screen -- is _DAT_53A9027C ever >1 in our use? If never, the whole per-player branch is dead and can be skipped in any port.


### FUN_52da9280

**Role:** widescreen_anchor -- the load-bearing "92-VA" (really 97 arithmetic writes across 93 distinct VAs) arithmetic-apply LEAF of Ephinea's widescreen cascade. Called exactly once from Stage C (FUN_52da7ff0) at call-site 0x52DA9062, AFTER Stage C's six bulk loops (122/90/35/28/235/12 iters) and ~80 scalar writes. It takes the engine's 4:3 (640x480 design-space) UI-anchor .data block 0x008F9Exx..0x008FA1Dx, plus 12 imm32 operands baked into PsoBB.exe .text (0x0040CAxx / 0x0040CBxx / 0x0040D0xx / 0x0040D1DD), plus one dll-internal scratch 0x5318E980, and rescales each anchor from 4:3 design-space to the live render size (gameRenderW x gameRenderH) so the front-end / char-select / dressing-room / congrats 2D layout fills the widescreen canvas. It is one of the 8 functions that actually drive Ephinea widescreen, but it is NOT the whole apply: Stage C writes ~700+ more globals around this call.

**Formula:**
```
Let W=gameRenderW(_DAT_5318deec), H=gameRenderH(_DAT_5318d124), DW=displayW(_DAT_5318d210), DH=displayH(_DAT_5318dcdc). Constants 640=_DAT_53176128, 480=_DAT_53176120, 2=_DAT_53175f20.

L1 (23):  *v = (W/640) * v
L2 (17):  *v = (H/480) * v
L3 (19):  *v = ((W-640)/2) + v
L4 (20):  *v = ((H-480)/2) + v
L5 (7):   *v = (H-480) + v          (NO /2)

INLINE (11, in body order, lines 132278-132292):
  0x008FA1D8 = (DW/W) * 0x008FA1D8
  0x008FA1D0 = (DH/H) * 0x008FA1D0
  0x008FA1D4 = (DW/W) * 0x008FA1D4
  0x008FA1CC = (DH/H) * 0x008FA1CC
  0x008FA1C4 = (DW/W) * 0x008FA1C4
  0x008FA1C0 = (DW/W) * 0x008FA1C0          (also an L1 member)
  0x5318E980 = (DW/640) * 0x5318E980        (DW/_DAT_53176128; dll-internal scratch)
  0x0040D039 = (DH/H) * 0x0040D039          (.text imm32)
  0x0040D032 = (DH/H) * (0x0040D032 - 480) + DH        (special: -480 then scale then +DH)
  0x0040D1DD = (DW/W) * ( (W-640)/2 + 0x0040D1DD )      (special: L3-add then DW/W scale)
  0x008FA1AC = (DH/H) * 0x008FA1AC          (also an L4 member)

NON-ARITH (2):
  *(u32*)0x0040D04E = &DAT_5318e980          (pointer-store / redirect, NOT arithmetic)
  FUN_52dc3460(0x0040D305, 0xB)              (installs an 11-byte engine .text code patch)

ORDER NOTE: 0x5318E980 is first scaled by (DW/640) in the inline block, THEN its ADDRESS is stored to 0x0040D04E -- the redirect must come AFTER the scale, exactly as the body does.
```

**Constants:** Divisor / scale constants are .rdata/.data globals referenced by the code but living OUTSIDE the 4.9MB code dump (referenced-only). Could NOT resolve their literal float bits from static artifacts: the radare2 MCP server is registered but its tool schemas are not surfaceable via ToolSearch this session (queries 'radare2'/'+r2'/'+radare2' returned no deferred-tool match), so I could not open the unpacked dll (ephinea_unpacked_base0x78480000.bin; file_addr = liveVA - 0x52d80000 + 0x78480000, e.g. 0x53175f20 -> 0x78875f20) to dump them. Values below are INFERRED (confidence=HIGH): the cascade is self-consistent only under them and the port + PATCHES_OVERVIEW.md sec4 #5 independently assert the same:
  _DAT_53176128 = 640.0     (STOCK_WIDTH)
  _DAT_53176120 = 480.0     (STOCK_HEIGHT)
  _DAT_53175f20 = 2.0       (HALF, L3/L4 letterbox divisor)
  _DAT_53175e8c = 1.33333 (4/3)   (Stage B aspect divisor; not read by this fn)
  _DAT_53175e64 = 1.0       (master scale; sibling detours, not this fn)
  _DAT_53175fc0 = 100.0     (HUDScale/100 in Stage B; not this fn)
Live render-size INPUTS are NOT constants -- they vary with resolution/HudScale (gameRenderW/H, displayW/H from Stages A/B). At true 16:9 / HudScale 1.0: displayW=gameRenderW=853.333, displayH=gameRenderH=480, so L1 factor=1.33333, L3 add=+106.667, L4/L5 add=0, every inline DW/W and DH/H ratio=1.0 (inline is IDENTITY at native HudScale; it only diverges when SSAA or non-1.0 HudScale make displayW != gameRenderW). Sample baked results from _ephinea_delta.csv quoted in PATCHES_OVERVIEW: 0x008F9EC0 100->133.333 (L1); 0x008F9F18 31->137.667 (L3, bits 0000f841->acaa0943); 0x008FA124 410->546.667 (L1).

**VAs touched (102):** L1 W-mul (23): 0x008FA154, 0x008FA15C, 0x008FA144, 0x008FA14C, 0x008FA134, 0x008FA13C, 0x008FA124, 0x008FA12C, 0x008FA0F8, 0x008FA098, 0x008FA068, 0x008FA0C8, 0x008FA1A4, 0x008F9EC0, 0x008FA1C0(dual L1+inline), 0x0040CA07(.text), 0x0040CA40(.text), 0x0040CA66(.text), 0x0040CA8C(.text), 0x0040CAE2(.text), 0x0040CB1E(.text), 0x0040CB44(.text), 0x0040CB6A(.text), L2 H-mul (17): 0x008FA148, 0x008FA150, 0x008FA128, 0x008FA130, 0x008FA0F4, 0x008FA0C4, 0x008FA0D4, 0x008FA094, 0x008FA0A4, 0x008FA064, 0x008FA074, 0x008FA044, 0x008FA04C, 0x008FA024, 0x008FA02C, 0x008FA104, 0x008F9EFC, L3 W-add (19): 0x008FA108, 0x008FA110, 0x008FA118, 0x008FA0D8, 0x008FA0E0, 0x008FA0E8, 0x008FA0A8, 0x008FA0B0, 0x008FA0B8, 0x008FA078, 0x008FA080, 0x008FA088, 0x008FA050, 0x008FA058, 0x008FA030, 0x008FA038, 0x008FA1D8(DOC-OMIT; dual L3+inline), 0x008F9F18(DOC-OMIT), 0x008F9F20(DOC-OMIT), L4 H-add (20): 0x008FA10C, 0x008FA114, 0x008FA11C, 0x008FA0DC, 0x008FA0E4, 0x008FA0EC, 0x008FA0AC, 0x008FA0B4, 0x008FA0BC, 0x008FA07C, 0x008FA084, 0x008FA08C, 0x008FA054, 0x008FA05C, 0x008FA034, 0x008FA03C, 0x008FA1D0(DOC-OMIT), 0x008FA1AC(DOC-OMIT; dual L4+inline), 0x008F9F1C(DOC-OMIT), 0x008F9F24(DOC-OMIT), L5 H-add-RAW (7): 0x008FA138, 0x008FA140, 0x0040CB07(.text), 0x0040CB2C(.text), 0x0040CB52(.text), 0x0040CB78(.text), 0x008F9F48, inline (11): 0x008FA1D4, 0x008FA1CC, 0x008FA1C4, 0x5318E980(dll scratch DW/640), 0x0040D039(.text DH/H), 0x0040D032(.text special), 0x0040D1DD(.text special), non-arith: 0x0040D04E(u32 ptr-store=&DAT_5318e980), 0x0040D305(FUN_52dc3460 11-byte code patch), READS: 0x5318deec(gameRenderW), 0x5318d124(gameRenderH), 0x5318d210(displayW), 0x5318dcdc(displayH), 0x53176128(=640), 0x53176120(=480), 0x53175f20(=2)

**Our impl:** PARTIAL MATCH with a DIFFERENT formula model + narrower scope. Our pso_widescreen.c reproduces the 0x008FAxxx half of FUN_52da9280 in TWO static-bake functions, both called from apply_static_patches (front-end relayout pass), value-guarded on exact stock float bits => idempotent, 4:3/hs1.0 byte-identical no-op:
 (1) patch_charselect_banner_center() [~L3868]: strides 11 center anchors by +half=(A-640)/2 -- covers a SUBSET of L3 (0x008FA108/110/118/0D8/0E8/0E0 + 0x008F9F18) plus non-FUN_52da9280 VAs (0x008FADC8, 0x00972148, .text Tab-key 0x0041061C/0x0041066B).
 (2) patch_charselect_vertical() [~L3949]: a 60-entry per-VA table over 0x008FA000..0x008FA1E0 with mode tags HMUL/KX/XHALF/XKX/YHALFH/YDH/AY/XKXAY/YHAY/XHAY. Mapping vs Ephinea:
   - HMUL (1.0 -> hud_scale) on the 16 L2 VAs == Ephinea L2 (1.0*gameRenderH/480 = hud_scale since gameRenderH=480*S). MATCH.
   - KX (1.0 -> hud*AR/(4/3)) on the 4 L1-scale VAs 0x008FA068/098/0C8/0F8 == Ephinea L1 (1.0*gameRenderW/640 = S*aspectMult/(4/3)). MATCH ONLY IF our baked game_aspect(16/9) == Ephinea aspectMult from its ladder.
   - XHALF (+half) == Ephinea L3 W-add. MATCH. XKX (stock*kx) on 0x008FA124/12C/13C/144/14C/15C/1A4 == Ephinea L1 W-mul for non-1.0 stock. MATCH.
   - YHALFH (+dh/2) == Ephinea L4 H-add. MATCH. YDH (+dh) on 0x008FA138/140 == Ephinea L5 H-add-raw. MATCH.
   - DIVERGENCE: AY/XKXAY/YHAY/XHAY apply affine_y=render_h/design_h to 0x008FA1C4/1CC/1D4/1C0/1AC/1D0/1D8. Ephinea applies the INLINE displayW/gameRenderW & displayH/gameRenderH ratios there, NOT a single render_h/design_h. At native HudScale (display==render) Ephinea's inline ratios=1.0 so those anchors are LEFT UNCHANGED by the inline pass (they only get their L1/L4 pass); our affine_y multiply is NOT identity unless render_h==design_h. Cleanest behavioral divergence to flag.
 NOT TOUCHED AT ALL by our impl: the 12 .text imm32 operands (0x0040CA07..0x0040CB6A L1, 0x0040CB07..0x0040CB78 L5, 0x0040D039/032/1DD inline), the dll-internal scratch 0x5318E980 (n/a -- inside ephinea.dll), the 0x0040D04E pointer redirect, the 0x0040D305 11-byte code patch. The 0x40CAxx/0x40CBxx group ARE real PsoBB.exe .text immediates that exist in our client and are currently UNPATCHED -> genuine eph_only gaps. We also do NOT run Stage C's ~700 surrounding writes (separate finding, owned by the anzz1 bake + per-frame in-game pin). Re-run: our pass runs at DllMain boot AND re-fires on every front-end<->in-game flip via the worker/relayout path (idempotent). We do NOT hook Device Reset (pso_widescreen.c:175 explicitly notes IDirect3DDevice8::Reset is not intercepted) -- same status as Ephinea (config-reload-proven, Device-Reset-unconfirmed).

**Port action:** 1. KEEP the single-boot + FE<->IG-flip idempotent bake model (patch_charselect_banner_center + patch_charselect_vertical) -- it correctly covers all 0x008FAxxx .data members of L1/L2/L3/L4/L5 and is value-guarded 4:3-no-op. Right architecture (single-boot idempotent bake, NOT per-frame).
2. FIX the inline-class divergence: replace the AY/XKXAY/YHAY/XHAY affine_y multiplier on 0x008FA1C4/1CC/1D4/1C0/1AC/1D0/1D8 with Ephinea's two-pass semantics: L1/L3/L4 pass first, THEN an inline pass multiplying by displayW/gameRenderW (1D8/1D4/1C4/1C0) or displayH/gameRenderH (1D0/1CC/1AC). At native HudScale display==render so the inline pass is identity and these keep only their L1/L4 result -- i.e. drop the affine_y term unless SSAA/non-native HudScale is active, where the true display/render ratio applies. Removes a value mismatch whenever render_h != design_h.
3. ADD the 12 PsoBB.exe .text imm32 operands we never touch: L1 *(renderW/640) at 0x0040CA07/40/66/8C/E2 + 0x0040CB1E/44/6A; L5 +(renderH-480) at 0x0040CB07/2C/52/78; inline at 0x0040D039 (*DH/H), 0x0040D032 (=(DH/H)*(v-480)+DH), 0x0040D1DD (=(DW/W)*((W-640)/2+v)). Each value-guarded on its exact stock imm bits; resolve stock floats live (or radare2 on the unpacked dll) before baking. Genuine eph_only gaps.
4. EVALUATE 0x0040D04E pointer-redirect + 0x0040D305 11-byte code patch: these touch ephinea.dll-internal flow (0x5318E980 scratch). Likely NOT portable / not needed (no DAT_5318e980 equivalent in our client); CONFIRM by disassembling stock bytes at 0x0040D305 in our PsoBB.exe -- if the 11-byte patch rewrites engine code that reads a 0x008FAxxx anchor, it is load-bearing.
5. RESOLVE constants when a live process or radare2 is available: dump _DAT_53176128/53176120/53175f20 (640/480/2 expected) and the stock float bits of all 12 .text immediates from the unpacked dll (file_addr = liveVA - 0x52d80000 + 0x78480000) to turn the inferred values into byte-verified ones.
6. TRACK the Device-Reset gap but do not add a hook: Ephinea's own Device-Reset rerun is unconfirmed; matching Ephinea exactly does not require a Reset hook today.

**Open:** Constant literals (_DAT_53176128=640, _DAT_53176120=480, _DAT_53175f20=2) are INFERRED not byte-verified -- the radare2 MCP tools were not surfaceable via ToolSearch this session so the unpacked dll (base 0x78480000) could not be dumped. Confidence HIGH (cascade self-consistency + port + PATCHES_OVERVIEW agree) but not proven from a raw read. | Does our baked game_aspect (16/9) always equal Ephinea's aspectMult from the Stage B 7-level breakpoint ladder? If the ladder buckets a near-16:9 display to a slightly different aspectMult, our KX/XKX L1 factor diverges from Ephinea gameRenderW/640. The breakpoint VALUES (1.30/1.45/.../5.25) are unverifiable-static (undumped .rdata, RPM-only). | The 0x0040D305 11-byte FUN_52dc3460 code patch: what 11 bytes does it install and which engine read does it rewrite? Unresolved statically -- needs a disasm of the stock bytes at 0x0040D305 in PsoBB.exe + the FUN_52dc3460 body. Until known, cannot decide if it is load-bearing for our port. | 0x5318E980 + its redirect to 0x0040D04E are ephinea.dll-internal -- confirm there is NO PsoBB.exe-side consumer that our client also needs (likely dll-only scratch, safe to skip, but unverified). | Does our patch_charselect_vertical T[] table cover the 0x008F9Fxx members 0x008F9F1C / 0x008F9F20 / 0x008F9F24 (L3/L4 DOC-OMIT entries)? They are NOT in the visible 0x008FA0xx..1Dx table -- confirm whether the 0x008F9Fxx members are baked elsewhere or are an additional gap. (0x008F9F18 IS covered by patch_charselect_banner_center.) | Whether Ephinea re-runs FUN_52da9280 on D3D Device Reset is config-reload-PROVEN but Device-Reset-UNCONFIRMED (no Reset hook found in Phase-1). Our impl also skips Reset -- matches Ephinea's confirmed behavior, but the unconfirmed path remains open for both.


### FUN_52da6e50

**Role:** widescreen_anchor / aspect_vertex -- the core per-draw 2D-vertex aspect-correct + recenter detour. One of only 4 VISUALLY load-bearing detour handlers (the other ~350 runtime detours are non-visual: localization/coop/input/camera/audio). Installed by FUN_52da6dd0 via FUN_52dc3240(FUN_52da6e50, 0x67c4ed, 5): a 5-byte detour of the engine `call 0x82b558` at site 0x0067C4ED. Runs an aspect-fit/recenter PRE-PASS over a freshly-built packed (x,y) f32 vertex array, then tail-calls the original submit helper 0x0082b558. Site 0x0067C4ED is inside a fullscreen 2D-overlay render (our codebase identifies it as the boss kill-screen / hexagon-layer fullscreen quad). 0x82b558 = RenderUIPrimitive_SetupAndDraw_0082b558 (low-level UI triangle-fan submit).

**Formula:**
```
bVar4 = (gameRenderW / gameRenderH) < (4/3).
IF bVar4 (taller-than-4:3, vertical-letterbox branch): k = gameRenderW / 640; off = gameRenderH/2 - (480*k)/2.
ELSE (wider-than-4:3, horizontal/pillar branch -- the 16:9 case): k = gameRenderH / 480; off = gameRenderW/2 - (640*k)/2.
Per vertex i in [0,vertCount): x_i = x_i * k; y_i = y_i * k; IF bVar4 then y_i = y_i + off ELSE x_i = x_i + off.
Then ((void(*)(int*,u4,u4,u4,int))0x82b558)(param_1, param_2, param_3, param_4, param_1[1]).
Note: 640/2 and 480/2 are written in the decompile as _DAT_53176128/_DAT_53175f20 and _DAT_53176120/_DAT_53175f20, i.e. divide by _DAT_53175f20=2.0 (the same HALF divisor Stage C uses for its L3/L4 half-delta anchors). The center offset is the classic half-margin: off = (longDim - shortStockDim*k)/2.
```

**Constants:** CONFIRMED via cross-reference (Stage B FUN_52dabbd0 usage + clean-port header ephinea_widescreen.h + PATCHES_OVERVIEW L90-97,113-117): _DAT_53176128=640.0f (STOCK_WIDTH); _DAT_53176120=480.0f (STOCK_HEIGHT); _DAT_53175e8c=1.33333f (4/3 ASPECT); _DAT_53175f20=2.0f (HALF divisor). gameRenderW/H are runtime values (=853.333*S / 480*S at 16:9), NOT static. UNRESOLVED-STATICALLY (as PATCHES_OVERVIEW warns): _DAT_5318deec and _DAT_5318d124 are .data slots populated at runtime by Stage B; I attempted to read them from the unpacked dll (file base 0x78480000; file offset = liveVA-0x52d80000) and got garbage (e.g. 5318deec bytes 6a10ff36, 5318d124 bytes 00008d42) -> these slots are uninitialized/zero in the static image, confirming they are referenced-only / runtime-written (cannot byte-verify statically). The .rdata consts 53175e8c/53176128/53176120/53175f20 ALSO read as garbage in the raw unpacked image at their computed offsets (the unpacked dump's .rdata/.data are not laid out at file-offset==liveVA-0x52d80000 for this region, OR the dump is code-only) -- so the 640/480/4-3/2.0 values are established by USAGE/CROSS-REF + Trinity/port, NOT by a direct byte read. Confidence on 640/480/4-3/2.0 = HIGH (three independent corroborations); confidence on exact runtime gameRenderW/H = these are formulas not constants.

**VAs touched (10):** READS _DAT_5318deec = gameRenderW (LIVE design render width; Stage B writes (640*S*aspectMult)/(4/3)); DLL-internal .data, runtime-populated, READS _DAT_5318d124 = gameRenderH (LIVE design render height; Stage B writes 480*S); DLL-internal .data, runtime-populated, READS _DAT_53175e8c = ASPECT_4_3 = 4/3 = 1.33333 (reference aspect, the < comparand); DLL .rdata const, READS _DAT_53176128 = STOCK_WIDTH = 640.0 (width divisor in taller-branch k, and stock-W in wider-branch off); DLL .rdata const, READS _DAT_53176120 = STOCK_HEIGHT = 480.0 (height divisor in wider-branch k, and stock-H in taller-branch off); DLL .rdata const, READS _DAT_53175f20 = HALF = 2.0 (the /2 center divisor); DLL .rdata const, READS+WRITES param_1[0] -> vertPtr: the packed f32 (x,y) vertex array, edited in place (x*=k,y*=k,+=off). This is a PsoBB engine STACK buffer at [esp+0x44..] built per-call by the 0x67C42B loop, NOT a static .data VA, READS param_1[1] = iVar2 (passed as 5th arg to 0x82b558); param_1[3] = iVar3 = vertCount (loop bound), CALLS engine 0x0082b558 = RenderUIPrimitive_SetupAndDraw_0082b558 (original submit, post-transform), INSTALL touches PsoBB .text site 0x0067C4ED (rewrites the 5-byte `call 0x0082b558`)

**Our impl:** PORTED (faithful, present and wired) -- this is the "additive aspect-fit splice FUN_52da6e50 @0x67C4ED" called out in MEMORY as the genuine remaining widescreen gap, and it HAS been implemented (2026-06-17). In pixelateds-psobb-mods/psobb-patches/atomic/pso_widescreen-asi/pso_widescreen.c: (1) ks_hexfit_transform(float* vptr, int n) @L3692 = the math port -- byte-faithful to FUN_52da6e50 (pillarbox = renderW/renderH<4/3 -> k=renderW/640, off=renderH/2-(480*k)/2, recenter Y; else k=renderH/480, off=renderW/2-(640*k)/2, recenter X; loop x*=k,y*=k,+=off). (2) ks_hexfit_shim @L3716 = naked CALL-site redirect that pulls vptr=desc[+0]/n=desc[+0xC], runs the transform, then `jmp 0x0082B558` with all args intact. (3) ks_hexfit_install @L3737 = redirect_call(0x0067C4ED, 0x0082B558, ks_hexfit_shim), called from apply_static_patches @L5800-5801 gated on `s->enabled && s->game_aspect > 4/3+0.001`. KEY DIVERGENCE (deliberate, documented @L3664-3679): our impl reads renderW/renderH as the LIVE DESIGN dims design_w=0x0098A4B8 (853.33*hud_scale) / design_h=0x0098A4B4 (480*hud_scale) via hs_peek_f32 PER-CALL, whereas Ephinea reads its own DLL-internal _DAT_5318deec/_DAT_5318d124. These are SEMANTICALLY the same (design-space render W/H), so the math matches; ours composes correctly because 0x82b558 then applies the global affine 0x00ACC0E8 (=render_w/design_w) and k=hud_scale cancels the affine -> hud_scale-invariant full-height centered result. The note explicitly records that an earlier build read PHYSICAL dims (g_scale.render_w/h) and double-scaled (~5x overscale); using design dims fixed it. Install is one-shot persistent (NOT in kStripSites), so unlike our code-flow hooks it is NOT wiped by the FE->IG transition -- matches Ephinea's persistent detour.

**Port action:** LARGELY DONE; remaining hardening to match Ephinea exactly: (1) IDEMPOTENCY/COMPOSITION are already correct -- the vert buffer is rebuilt per-frame on the stack so in-place *=k;+=off cannot accumulate, and the redirect is one-shot persistent. Keep it. (2) RE-RUN ON CONFIG-RELOAD / DEVICE-RESET: Ephinea's transform is data-driven (reads _DAT_5318deec/d124 every call) so it auto-tracks config reload; OUR impl is ALSO per-call data-driven (hs_peek_f32 of design_w/h every call), so it likewise auto-tracks a hud_scale/aspect change WITHOUT needing re-bake of the detour itself -- no extra action needed for the transform. The ONLY re-assert concern is the .text redirect byte itself: since 0x0067C4ED is NOT a code-flow hook wiped by FE->IG (confirmed: it is treated like the persistent .text affine bakes, NOT in kStripSites), it survives; verify this empirically on a real FE->IG flip and on a D3D Device Reset (Ephinea's Reset re-run is UNCONFIRMED per Phase-1, so don't assume -- but our redirect persists regardless). (3) GATE PARITY: we install only when game_aspect>4/3+0.001 (4:3 is identity because else-branch gives k=1,off=0) -- matches Ephinea behavior (no-op at 4:3). (4) VERIFY LIVE: this site only fires on the boss kill-screen / hexagon fullscreen overlay; it has never been observed firing under our test conditions (no PSO server / boss-kill reachable), so the on-screen result is UNVERIFIED -- needs a live boss-kill capture to confirm the fullscreen layer fills height-fit + centered (margins ~240px @1920) and is hud_scale-invariant. (5) Optional exactness: Ephinea passes param_1[1] as the explicit 5th arg to 0x82b558; our shim tail-jmps leaving the original cdecl stack intact so the engine caller's existing arg layout + `add esp,0x1E4` cleanup is preserved -- equivalent, but re-confirm the 5th-arg (iVar2=param_1[1]) is already on the stack at the original call site so behavior is byte-identical.

**Open:** Direct byte-verification of _DAT_53176128/53176120/53175e8c/53175f20 (640/480/4-3/2.0) FAILED from the unpacked dll at file_offset=liveVA-0x52d80000 (read garbage) -- the unpacked image's .rdata/.data either is not at that file mapping or is code-only. Values are HIGH-confidence via 3 cross-refs (Stage B usage, port header, PATCHES_OVERVIEW) but NOT a raw byte read. To byte-confirm, need r2 with the correct section-to-file mapping of the unpacked dll, or a live read. | _DAT_5318deec/_DAT_5318d124 are runtime-populated by Stage B (FUN_52dabbd0); statically unconfirmable (confirmed zero/uninit in the static image). Their runtime magnitude (853.333*S / 480*S at 16:9) is inferred from Stage B's formula, not measured. | Trinity resolution.h does NOT name site 0x0067C4ED or fn 0x82b558 directly (it is a static patcher; this fullscreen-overlay aspect-fit is a runtime-only transform with no single .data anchor VA for Trinity to label). The closest Trinity analog is its valScaleI/valAspectRatio Start-Screen tile tables -- different mechanism (static tile-size scaling) not the same per-draw vertex pass. So no Trinity divergence to reconcile for THIS fn (unlike the dressingRoom tiles). | Whether the same FUN_52da6e50 detour at 0x67C4ED also covers other fullscreen 2D overlays beyond the boss kill-screen hexagon layer (the site is named generically; our codebase labels it kill-screen/congrats). If other fullscreen layers route through the same 0x82b558 call site, our redirect would transform them too -- needs a live draw_capture at 0x67C4ED to enumerate callers (blocked: no live process here). | On-screen RESULT of our port is UNVERIFIED -- the kill-screen path has not been reached under test (no boss-kill reachable). Needs a live boss-kill capture to confirm height-fit+centered, hud_scale-invariant, no double-scale, and that the layer is not clipped. | Ephinea re-run on D3D Device Reset is plausible-but-unconfirmed (Phase-1 found no Reset hook). Our redirect persists across Reset by being a static .text byte, but if a Device Reset ever re-pages .text from disk (it does not, normally) the redirect could revert -- low risk, worth a note.


### FUN_52dc4f60

**Role:** widescreen_anchor / hud_quad (TEXT sub-path). Per-draw DETOUR wrapper that is the front door of Ephinea's reimplemented on-screen STRING/GLYPH rasterizer. Installed by the cascade installer FUN_52dc4bd0 via FUN_52dc3240(FUN_52dc4f60, 0x78A300, 5) = a 5-byte E9 detour at PsoBB site 0x0078A300 (the engine text-draw entry; 0x28 before UpdateJapaneseCharacterDisplayOption_0078A328; the stock text path is the RenderText3D_007AC12C family). It reads the LIVE 2D design-anchor scale (0x00ACC0E8/EC = the same global 2D affine SCALE_X/Y our harness already forces wide), decides whether the glyph batch needs an extra design-anchor multiply, builds an optional scale-pin pointer, sets a 'param6 != sentinel' boolean, and tail-forwards to the real per-glyph layout/raster worker FUN_52dc5b30. One of only 4 VISUAL per-draw detours (with FUN_52da6e50 aspect-vertex, FUN_52d8cb90 ui-edge, FUN_52db2240 deanchor-pin).

**Formula:**
```
PER-CALL PIN (FUN_52dc4f60):
  a = *0x00ACC0E8 ; b = *0x00ACC0EC                          // live affine SCALE_X / SCALE_Y
  sx = sy = M (=_DAT_53175e64 ~ 1.0) ; pin = NONE
  if (M < a && M < b):  sx = M*a ; sy = M*b ; pin = (sx,sy)  // GUARD 1 (widescreen/HudScale up)
  if (T1(=_DAT_53175ec8) < D(=_DAT_5318e34c)): sx*=D ; sy*=D ; pin = (sx,sy)   // GUARD 2 (density/SSAA)
  shadowFlag = (param6 != S(=_DAT_53175df8))
  FUN_52dc5b30(..., shadowFlag, ..., pin)

PER-GLYPH QUAD EXPANSION inside FUN_52dc5b30 (only when pin != NULL && pin.x>T2(=_DAT_53175df0) && pin.y>T2):
  w = x2-x1 ; h = y2-y1
  x2 = x2 + pin.x * w
  y1 = y1 - pin.y * h / K(=_DAT_53175f20)        // K ~ 2.0 -> symmetric vertical growth
  y2 = y2 + pin.y * h / K
(glyph base size/advance come from the font-cell scale _DAT_5318f330/33c = fontHeight/_DAT_5317605c and advance _DAT_5318f324, set by FUN_52dc5570). Net: text grows by ~(1+pin) about its anchor when the global affine is wide; identity (pin=NULL) when affine==1.0.
```

**Constants:** UNRESOLVABLE FROM THE ARTIFACT (confidence=low, stated honestly): every _DAT_5317xxxx / _DAT_5318xxxx literal in this function lives in ephinea.dll's own .rdata/.data, NOT in PsoBB. I attempted to resolve them against the unpacked dll (C:/Users/u03a9/Repositories/llama-bob-ephinea-re/unpacked/ephinea_unpacked_base0x78480000.bin, a real PE rebased to image-base 0x78480000). Mapping liveVA(0x52d80000-based) -> dump RVA (= liveVA-0x52d80000) lands these symbols at RVA 0x3F5E64 / 0x40E34C, which falls INSIDE the dump's .text section (raw bytes decode as code: 8b4d/894d/8b45...), i.e. the decompiler's data base for these symbols is NOT 'liveVA-0x52d80000' relative to THIS dump's 0x78480000 rebase. This exactly matches the Phase-1 warning that _DAT_5317xxxx/5318xxxx 'live in .data/.rdata possibly OUTSIDE the code dump'. radare2 MCP did not surface as a callable tool in this session, so I could not cross-confirm via aflx/px. INFERRED values from code role (confidence=low): _DAT_53175e64 = 1.0f (master/identity scale; both guards compare 'scale < anchor' and multiply by it as a unit); _DAT_53175ec8 and _DAT_53175df0 = small thresholds near 1.0 (activation gates); _DAT_53175f20 = 2.0 (Y pin is split half above/half below the anchor: y1-=p*h/_; y2+=p*h/_); _DAT_5317605c = the reference font-cell height (the divisor making fontHeight/ref == 1.0 at design size; given the 14..72 clamp, ref is most likely ~14.0 or the design cell). param6 sentinel _DAT_53175df8 INFERRED ~0.0f. CONFIRMED non-dll constants: 0x00ACC0E8/EC are the verified PsoBB 2D affine; the install is byte-exact 5-length E9 at 0x0078A300; the DAT_531761e0 reference is the {0.0, 4294967296.0} int->double table (compiler idiom, not a layout const).

**VAs touched (14):** READS 0x00ACC0E8 (2D affine SCALE_X / designAnchorX) — via FUN_52dd2610; CONFIRMED PsoBB VA (our memory: the in-game/front-end 2D affine), READS 0x00ACC0EC (2D affine SCALE_Y / designAnchorY) — via FUN_52dd2610; CONFIRMED PsoBB VA, READS _DAT_53175e64 (master scale, ==1.0f identity) — ephinea.dll .rdata, UNRESOLVED from artifact, READS _DAT_53175ec8 (GUARD-2 threshold for the density/SSAA factor) — ephinea.dll .rdata, UNRESOLVED, READS _DAT_5318e34c (runtime extra-scale/density factor; written by installer FUN_52dc4bd0 L145993 = (float)local_20 from screenW/refW ratio) — ephinea.dll .data, READS _DAT_53175df8 (param6 sentinel for the mono-advance/shadow boolean) — ephinea.dll .rdata, UNRESOLVED, INSTALL TARGET 0x0078A300 (PsoBB text-draw entry; 5-byte E9 detour; site label = just before UpdateJapaneseCharacterDisplayOption_0078A328), WRITES: NONE (FUN_52dc4f60 writes no .data; it only builds a stack-local scaleX/scaleY pin and passes its address), --- downstream FUN_52dc5b30 / FUN_52dc5570 globals (the actual data this path consumes) ---, READS 0x00AAB904/0x00AAB908/0x00AAB90C/0x00AAB910 (text CLIP-RECT L/T/R/B) + 0x00AAB914 (clip enable) + DAT_00AAB91C (glyph color/flags) — PsoBB live globals, READS/uses _DAT_5318f330 (glyph cell scale X) and _DAT_5318f33c (glyph cell scale Y) = fontHeight/_DAT_5317605c — ephinea.dll .data, set by FUN_52dc5570, READS _DAT_5318f324 (glyph advance = (fontHeight+5)*0x10), _DAT_5318f338 (line height), _DAT_5318f340 (half-cell), _DAT_5318e348 (font px height, clamp 14..72) — ephinea.dll .data, READS _DAT_53175df0 (pin activation threshold, pin[i] must exceed it), _DAT_53175f20 (Y half-divisor for the pin expansion), _DAT_5317605c (font reference-cell divisor) — ephinea.dll .rdata, UNRESOLVED, palette 0x0097DA10 (color-escape table) + 0x00A98478 (current-color object) — PsoBB; emitter primitives called by absolute VA: 0x82BB60, 0x7ABA94, 0x7A9044, 0x82DBAC

**Our impl:** MISSING (the dedicated text-path scaler) but FUNCTIONALLY COVERED by a different mechanism. Our pso_widescreen.c (C:/Users/u03a9/Repositories/pixelateds-psobb-mods/psobb-patches/atomic/pso_widescreen-asi/pso_widescreen.c, 6459 lines) installs NO hook at 0x0078A300 and does NOT touch the per-glyph font-cell scale globals (grep for 0x78a300 / 5318f330 / text-draw-hook = zero hits). Instead we drive the SAME upstream input this function reads — the global 2D affine 0x00ACC0E8/EC — directly to the wide value (ig_aff / F_VPRESET poke at 0x0082F8F4 'mov [0x00ACC0E8],imm'; deanchor save/restore g_deanchor_save_x; in-game ingame_scale_pin forces SCALE = render_w/design_w, e.g. 2.25@hs1.0 / 1.5@hs1.5). Because FUN_52dc4f60 DERIVES its pin from 0xACC0E8/EC, our wide affine makes the stock (un-detoured) PsoBB text path scale too — so on-screen text DOES enlarge with widescreen in our build. The architectural DIFFER: Ephinea applies the text scale as an explicit per-glyph-quad geometric pin inside a reimplemented rasterizer (clip-aware, density-factor-aware, drop-shadow boolean); we apply it implicitly via the shared 2D affine. Known consequence already in our notes: this is exactly the 'in-game font physically larger / 1.333x too wide / 4px overlap' class of issue (memory: ingame-font-x-stretch-rootcause-fix, ingame-font-x-stretch) — i.e. without Ephinea's per-glyph advance/clip recompute, sharing one affine for both geometry and font-cell can mis-stretch glyph WIDTH vs advance. Trinity (same build, static) ALSO has no equivalent: it has the design-resolution ladder (854/768x480) and named X/Y anchor tables but NO per-glyph font-scale anchor for this path — confirming this scaler is a runtime-detour-only construct, not a static .data patch.

**Port action:** DECISION FIRST: this is a per-draw scale DERIVED from 0xACC0E8/EC, not a bakeable .data constant, so a single-boot idempotent .data bake CANNOT replicate it; matching Ephinea exactly requires reproducing the per-glyph behavior. Two viable paths: (1) KEEP our affine-shared approach (cheapest, already mostly works) and only fix the residual glyph WIDTH-vs-advance mismatch in our existing RenderUIQuad hook (pb_fix_bar) by un-stretching glyph quad width x (4/3)/aspect (already shipped per ingame-font-x-stretch-rootcause-fix) — accept that we differ architecturally from Ephinea but match visually. OR (2) For pixel-parity, port FUN_52dc4f60 + the FUN_52dc5b30 pin block: install a 5-byte detour at PsoBB 0x0078A300 that computes pin = (M*aff.x, M*aff.y) with M=1.0, applies the GUARD-2 density factor only if we replicate _DAT_5318e34c (we do not currently compute an SSAA factor, so GUARD 2 can be omitted -> pin = aff.x/aff.y directly), and on the existing glyph-quad submit re-expands the quad as x2+=pin.x*w; y1-=pin.y*h/2; y2+=pin.y*h/2. Either way: must RE-RUN on config-reload (Ephinea re-runs the whole cascade via the tail-call chain on reload — proven) and SHOULD re-assert on front-end<->in-game transition (our .text hooks get wiped on that flip per ingame-hudscale-dead-worker-thread-fix; drive it from the ASI worker thread). BLOCKERS to do FIRST (cannot finalize a byte-exact port without these): (a) resolve _DAT_53175e64 (confirm ==1.0), _DAT_53175ec8/_DAT_53175df0 thresholds, _DAT_53175f20 (confirm ==2.0), _DAT_5317605c (font reference height) and _DAT_5318e34c semantics — via a LIVE read of ephinea.dll (launch_ephinea + read_memory at the live 0x5317xxxx VAs) since the static unpacked-dll mapping does NOT resolve them; (b) live-confirm at a breakpoint on 0x0078A300 / FUN_52dc5b30 that param_8 pin is non-NULL in widescreen and NULL at 4:3, and capture the actual pin values vs our affine. Until (a)+(b), keep path (1) and do NOT add a redundant text-scale detour (would double-scale on top of the wide affine).

**Open:** Exact values of _DAT_53175e64 (master scale, presumed 1.0), _DAT_53175ec8 / _DAT_53175df0 (activation thresholds), _DAT_53175f20 (Y pin divisor, presumed 2.0), _DAT_5317605c (font reference-cell height divisor), _DAT_53175df8 (param6 sentinel) — UNRESOLVED from the static unpacked dll (RVA mapping lands in .text, not .data; radare2 MCP not callable this session). Needs a live ephinea.dll read. | Semantics/source of _DAT_5318e34c (GUARD-2 density factor): installer FUN_52dc4bd0 sets it to (float)(screenW-ratio) at L145993 only inside an SSAA branch (local_20 = screenW/refFontW). Is GUARD 2 ever true in plain widescreen (no SSAA)? If the factor stays 1.0/0.0 then only GUARD 1 (the affine) drives text scale — which is what our affine-shared approach already does. Needs live confirm. | Does FUN_52dc4f60's pin DOUBLE-COUNT with our wide affine? Ephinea's text path is its OWN reimplementation (it does not also hit stock 0xACC0E8 geometry the same way). If we port the detour ON TOP of our already-wide affine we risk scaling text twice. Must establish at a live bp whether porting replaces or stacks. | Is the param6!=sentinel boolean a drop-shadow toggle, a mono-advance/centered toggle, or a color flag? FUN_52dc5b30 routes \n by it (param6==0 -> Y newline advance by line-height; else X advance by half-cell), so it looks like a vertical-vs-horizontal text-flow / shadow-pass selector — confirm by tracing the two call sites' param6. | Is 0x0078A300 the SOLE text-draw chokepoint, or does Ephinea also detour RenderText3D_007AC12C (0x007AC12C) and the 0x7AB554/0x7ABAA5/0x7AC1A0 glyph cluster separately (PATCHES_OVERVIEW lists those as separate char-select/glyph detours)? If the latter, FUN_52dc4f60 covers only one of several text paths and a faithful port needs all of them. | Re-run on D3D Device Reset: Phase-1 found NO visible Reset hook; does the font-cell setup FUN_52dc5570 (which feeds 5318f330/33c) re-run on Reset, or only on config reload? Affects whether our re-assert needs a Reset trigger.


### FUN_52d8cb90

**Role:** widescreen_anchor — per-draw UI edge re-anchor; specifically the IN-GAME ListWindow bottom-anchor. One of the 8 visual widescreen drivers. Installed by FUN_52d8caf0 as a 5-byte E9 detour at PsoBB.exe site 0x0073FF92 (in _ephinea_hooks.txt: "0x73ff92 handler=FUN_52d8cb90 len=5"). That site is the `call 0x00737F80` inside ListWindowObject_Create_0073ff34, in the SINGLE-PLAYER arm (maybe_num_split_screen_players==1). Original 0x00737F80 = ListWindowObject_InitBase(undefined4* xy, id), which stores xy[0]->[0x009F3130](baseX), xy[1]->[0x009F3134](baseY), id->[0x009F3128]. In that arm the {X,Y} arg is &item_bank_and_quest_list_dimensions (0x00972DEC, design {385.0, 184.0}) = the construction-time base position of the in-game item-bank / bank / shop / quest-counter LIST WINDOW. The detour recomputes Y to track the TALLER widescreen viewport bottom edge (a MOD_Y_B "480-HUD_H" pattern), substitutes a live left-edge X, applies a live-screen-bounds clamp, then forwards a substituted scratch vec2 {x,y'} to the original InitBase. NB: 0x00737F80 / 0x009F3130 / the gate flag 0x00A95EDC are PsoBB.exe VAs; the 0x5317xxxx/0x5318xxxx/0x5382xxxx symbols are ephinea.dll runtime data.

**Formula:**
```
Widescreen ON: Y' = (gameRenderH - 480.0) + Yoffset, gameRenderH = 480.0*S, Yoffset(_DAT_5317610c)=334.0  =>  Y' = design_h - 146 (S = HUDScale/widescreen factor; design_h = 480/720@1.5/960@2.0). X' = *_DAT_5318d10c (live viewport-left). Bounds clamp: if (*_DAT_5318d108 - PAD <= *_DAT_5318d114 + *_DAT_5318d10c) then Y' = (_DAT_5318d108[1] - _DAT_5318d114[1]) - PAD, PAD=_DAT_53176060. Forward {X',Y'} (scratch 0x5382fe60/64) to ListWindowObject_InitBase(0x00737F80). Widescreen OFF: pass original {385,184} unchanged. Sibling X-clamp (FUN_52d8cca0): X = max(srcX, *_DAT_5318d114 + *_DAT_5318d10c + _DAT_5318d11c).
```

**Constants:** _DAT_53176120 = 480.0f (STOCK_HEIGHT) — CONFIRMED: header ephinea_widescreen.h labels it 480.0; corroborated by the Stage-B write `_DAT_5318d124 = _DAT_53176120 * S` (gameRenderH = 480*S). _DAT_5318d124 = gameRenderH = 480.0*S (runtime, not a literal). _DAT_5317610c (Yoffset) = 334.0f — RESOLVED via live read of a running Ephinea (memory listwindow-yanchor-ephinea-verified-offby150, account Zoe); it is entry 0 of an Ephinea .rdata table [334,353,378,384,400,480,576,640]; so widescreen-on Y' = design_h - 480 + 334 = design_h - 146 (Ephinea re-bases at 4:3 too: design_h=480 -> Y=334, NOT stock 184). _DAT_53176060 (clamp PAD) = UNRESOLVED literal — referenced-only, lives in unpacked-dll .data not present as init data. _DAT_5318d11c (sibling X-inset) = UNRESOLVED literal. The three pointer symbols _DAT_5318d108/10c/114 are NOT floats — they are runtime-resolved POINTERS to the live D3D screen-rect (per memory note: _DAT_5318d10c->0x00973CEC, _DAT_5318d108->~0x00972FB0, _DAT_5318d114->~0x00972F80, all PsoBB globals). STATIC-RESOLUTION FAILED: I parsed the unpacked dll PE (C:/Users/u03a9/Repositories/llama-bob-ephinea-re/unpacked/ephinea_unpacked_base0x78480000.bin, imagebase 0x78480000, 10 sections, a .banana packed section present). The data VAs (live 0x5317xxxx/0x5318xxxx -> dll 0x7887xxxx/0x7888xxxx) all map into the FIRST .text section and read as code/garbage, NOT initialized data — confirming Phase-1's warning that these _DAT_5317/5318xxxx live in runtime-populated .data/.bss outside the static dump. radare2 MCP did not surface as a callable tool this session. The only literals I can assert with confidence: 480.0 (subtrahend) and 334.0 (Yoffset, live-verified). 53176060 and 5318d11c remain unconfirmable from the artifacts.

**VAs touched (17):** 0x0073FF92 = detour install site (E9 to FUN_52d8cb90); the ==1-arm `call 0x00737F80`, 0x00737F80 = ListWindowObject_InitBase (the original, called through both branches), 0x009F3130 / 0x009F3134 / 0x009F3128 = InitBase output globals (baseX / baseY / id) written downstream by 0x00737F80, 0x00972DEC = item_bank_and_quest_list_dimensions = the {385.0,184.0} design vec2 passed in the ==1 arm (the xy* source), 0x00A95EDC = _DAT_00a95edc = ephinea widescreen-ACTIVE dynamic flag (PsoBB VA; ==0 -> pass-through), _DAT_5318d124 = gameRenderH (= 480.0 * S; the only widescreen-scaled input; written by Stage-B @ _ephinea_decompiled.c:133478), _DAT_53176120 = 480.0f STOCK_HEIGHT (the subtrahend; const), _DAT_5317610c = Yoffset added after the delta (live-verified = 334.0; entry 0 of an ephinea .rdata table [334,353,378,384,400,480,576,640]), _DAT_5318d108 = pointer to live screen-rect struct (deref [0]=top/origin-Y test value, [1]=bottom in clamp); live VA ~0x00972FB0 per memory note, _DAT_5318d114 = pointer to a second live rect (deref [0] used in bounds test, [1] in clamp); live VA ~0x00972F80 per memory note, _DAT_5318d10c = pointer to live X-base / left viewport edge (scratch.x = *_DAT_5318d10c); maps to PsoBB *(float*)0x00973CEC per memory note, _DAT_53176060 = PAD/inset constant subtracted in the bounds test and clamp (UNRESOLVED literal), _DAT_5318d11c = additive inset used by the SIBLING X-clamp FUN_52d8cca0 (UNRESOLVED literal), _DAT_5318e458 = vtable/fn-ptr indirect-called by sibling FUN_52d8cca0 to fetch the source xy ((*_DAT_5318e458)(param)), DAT_5382fe60 / DAT_5382fe64 = ephinea scratch vec2 {x,y'} (the substituted arg forwarded to InitBase), DAT_5382fe58 / DAT_5382fe5c = sibling scratch vec2 for FUN_52d8cca0, (related, not this fn) FUN_52dac110 @0x52dac110: 0x53146308 = 0x14-entry list-object offset-table base; _DAT_5318deec = gameRenderW; _DAT_5317612c = 640.0 stock-width subtrahend

**Our impl:** PRESENT — faithful-EFFECT port (not byte-faithful). pso_widescreen.c (C:/Users/u03a9/Repositories/pixelateds-psobb-mods/psobb-patches/atomic/pso_widescreen-asi/pso_widescreen.c):
- lw_yanchor_compute (~L3794): computes Y' = design_h(0x0098A4B4) - 480.0 + 334.0 = design_h - 146 (matches Ephinea's verified C1=480/C2=334 after the off-by-150 fix shipped md5 BBCB128E; superseded the original wrong base 184). KEEPS X = stock design 385 unconditionally (the live-X track via *0x00973CEC was added then REVERTED md5 297796F9 — disasm showed 0x00973CEC is a code-address table entry at the front-end, not a coordinate; derefing it snapped the window far-left).
- lw_yanchor_shim (naked, ~L3819): rewrites the on-stack xy arg to &g_lwy_scratch and tail-jmps 0x00737F80.
- lw_yanchor_redirect (~L3840): stock-guarded idempotent install of the E8 call-redirect at 0x0073FF92.
- Re-asserted every in-game worker tick in reassert_ingame_hooks (~L6106, gated g_cfg.enabled) because the FE->IG transition wipes .text code-flow hooks.
DIVERGENCES from Ephinea: (1) we DROP the live-bounds CLAMP branch (Ephinea's *_DAT_5318d108/_DAT_5318d114 deref) — deliberate, those are DLL-private live-rect pointers we couldn't safely map; the clamp only matters at extreme aspect ratios. (2) we DROP the live left-edge X substitution (*_DAT_5318d10c) and keep stock X=385 — Ephinea's X-drop is MATERIAL in widescreen (it tracks the viewport left edge) so our window stays pinned to the 4:3 X slot; this is a KNOWN remaining gap, blocked on a live in-game bank/shop read to find the true viewport-left global. (3) we gate on our own design_h>480 (self-no-op at hud_scale 1.0) rather than Ephinea's private 0x00A95EDC flag. Y bottom-anchor (the dominant effect) MATCHES Ephinea exactly at all hud_scales.

**Port action:** Y bottom-anchor: ALREADY MATCHES — keep Y' = design_h - 480 + 334 (= design_h - 146). No change needed; it is a single-boot redirect that self-scales via design_h (which already encodes hud_scale), idempotent, value-guarded, and re-asserted on every FE<->IG flip (correct analog of Ephinea's re-run-on-config-reload via the Stage tail-call chain). REMAINING to match Ephinea EXACTLY: (1) restore the live left-edge X substitution. Ephinea sets scratch.x = *_DAT_5318d10c (the live viewport-left). Our build's equivalent global must be found via a LIVE in-game bank/shop ListWindow read at 16:9 (0x00973CEC was a mis-map — it is a code-address table entry, denormal-as-float, would snap far-left). Blocked until a PSO server is reachable. Until then keep stock 385 (never regresses). (2) optionally port the bounds CLAMP: y' = (rect.bottom - rect2.bottom) - PAD when (rect.top - PAD <= rect2.something), to keep the window on-screen at extreme aspects — low priority, needs the live-rect globals (~0x00972FB0/0x00972F80) confirmed live and the PAD const (_DAT_53176060) resolved. (3) Consider the SIBLING family for parity: FUN_52d8cca0 (left-edge X-clamp at 0x6daa37/0x6d79ee/0x6da9a4/0x6d7ab6) and FUN_52d8cc60 (0x6d78bd/0x6da5d0) re-anchor other ListWindow members on the SAME live-rect set — we do NOT port these; evaluate whether bank/shop sub-windows drift without them. (4) Optionally port FUN_52dac110 (engine-wide ListWindow-entity offset-table reanchor, same renderH-480 delta) if list-entity rows (not just the base) drift in-game. Do NOT touch the ==2 / ==3-4 split-screen arms (0x0073FF73 / 0x0073FFF9) — Ephinea hooks only the ==1 arm; we match.

**Open:** Literal values of _DAT_53176060 (clamp PAD) and _DAT_5318d11c (sibling X-inset) are UNRESOLVED — referenced-only data in the packed dll's runtime .data, not in the static dump; radare2 MCP was unavailable this session. Resolve via a live ephinea.dll read (parse the movss absolute operands of FUN_52d8cb90/FUN_52d8cca0 at their live engine addresses reached through the 0x0073FF92 detour) or via the live data-VA delta technique (~0x260C2000). | Our build's TRUE viewport-left global (Ephinea's _DAT_5318d10c -> *0x00973CEC) is unknown for the IN-GAME case — 0x00973CEC is a code-address-table entry at the front-end. Needs a live 16:9 in-game bank/shop ListWindow read (blocked: no reachable PSO server). The X-edge drop is the one material remaining gap vs Ephinea. | Are the live-rect pointers _DAT_5318d108 (~0x00972FB0) and _DAT_5318d114 (~0x00972F80) actually populated in-game in psobb.io, and is the clamp branch ever taken at 16:9/21:9? If never taken at normal aspects, our clamp-drop is harmless (current assumption). | Does Ephinea re-run FUN_52d8cb90's install on D3D Device Reset (not just config reload)? Phase-1 found no Reset hook; our per-tick re-assert covers FE<->IG wipes but a mid-game Reset re-entry path is untested. | Do bank/shop SUB-windows or list ENTITY rows drift in-game without porting the siblings (FUN_52d8cca0/FUN_52d8cc60) and FUN_52dac110? Needs a live in-game capture to decide if those are required for full parity. | Minor: memory note cites C1 @0x53176128 but the decompile subtrahend is _DAT_53176120 (0x53176128 = STOCK_WIDTH 640.0 per header). Value 480.0 is consistent; the note's VA citation for C1 appears slightly off — worth reconciling if byte-exact VA tracking is needed.


### FUN_52db2240

**Role:** deanchor

**Formula:**
```
No arithmetic. Pure save/pin/restore bracket:
  saveX = [0x00ACC0E8]; saveY = [0x00ACC0EC]
  [0x00ACC0E8] = 1.0f ; [0x00ACC0EC] = 1.0f        (1.0f sourced from _DAT_53175e64)
  if (arg2 != 0) call 0x0082B158(arg1,arg2,arg3,arg4)
  [0x00ACC0E8] = saveX ; [0x00ACC0EC] = saveY
The "pin" value is a literal 1.0f regardless of HudScale/widescreen factor — the wrapped draws render in native 2D design space; the surrounding wide affine is only restored AFTER the submit. 0x0082B158 (-> emitter 0x00836D04 / RenderUIQuad family) is the function that actually multiplies the affine into the 2D verts.
```

**Constants:** _DAT_53175e64 = 1.0f (the pin value). CONFIDENCE = HIGH but resolved by TRIANGULATION, not by direct dword read. Three independent sources agree it is 1.0f: (1) PATCHES_OVERVIEW.md line 347 lists "0x53175e64=1.0"; (2) the clean port deanchor_pin.c line 23-24 annotates `*(float*)0xACC0E8 = _DAT_53175e64; /* = 1.0f */`; (3) usage semantics across the dump — _DAT_53175e64 is used as a divisor (x/_DAT_53175e64), additive identity (x±_DAT_53175e64), and comparator (_DAT_53175e64 <= val) in aspect/normalize math, all consistent with 1.0f, and our own pso_widescreen.c writes the literal 0x3F800000 (=1.0f) at exactly these sites.
RADARE2/RAW-DUMP RESOLUTION = UNRESOLVED/UNCONFIRMABLE from the unpacked dll: the supplied formula file_addr = liveVA - 0x52d80000 + 0x78480000 maps 0x53175e64 -> file VA 0x78875e64 (RVA 0x3F5E64), but reading there (and via proper PE RVA->raw mapping) yields garbage floats — the `.banana`-packed unpacked snapshot (10 sections, names stripped, image_base 0x78480000) did NOT preserve the .rdata/.data constant pool at the linearly-translated offset (the data section has VSize 0x115d000 but RawSz only 0x4200; section 0 raw covers the float region but the constant pool was relocated by the packer, so the simple rebase does not land on it). The canonical bit patterns DO exist in the file (1.0f/0x0000803f appears 1169x, 640.0f 6x, 480.0f 10x, 1.3333f 1x) confirming the pools are present, but pinning the EXACT offset of 0x53175e64 needs the relocation map, not recoverable from these static artifacts alone. So: value 1.0f = HIGH confidence by triangulation; direct-byte radare2 confirmation = LOW/unavailable (stated per Phase-1 caveat that _DAT_5317xxxx lives in .rdata possibly outside/relocated in the dump).
Related constants seen in sibling fns (same .rdata pool, for context, same unresolved-by-rawdump caveat): _DAT_53176128=640.0, _DAT_53176120=480.0, _DAT_53175e8c=4/3, _DAT_53175f20=2.0, _DAT_53175ec8=1.0(double). The pin/strip literal applied in our code is 0x3F800000.

**VAs touched (9):** 0x00ACC0E8 (engine global SCALE_X — 2D design-anchor affine X; saved, pinned to 1.0f, restored), 0x00ACC0EC (engine global SCALE_Y — 2D design-anchor affine Y; saved, pinned to 1.0f, restored), 0x0082B158 (engine 2D vertex-submit helper; called indirectly with the 4 forwarded args; the affine is multiplied into verts inside it / its emitter 0x00836D04), 0x004A9C0C (hook site 1: stock `call 0x0082B158`, redirected to FUN_52db2240), 0x004A9D28 (hook site 2: stock `call 0x0082B158`, redirected), 0x005474D4 (hook site 3: stock `call 0x0082B158`, redirected), _DAT_53175e64 (ephinea.dll .rdata float const = 1.0f — the pin value), FUN_52dc3240 (the 0xE8 CALL-rel32 detour installer used to wire the 3 sites), FUN_52db05f0 (the patch-table installer that contains the 3 FUN_52dc3240(FUN_52db2240,...) calls)

**Our impl:** MATCH (functionally byte-faithful), implemented as a per-frame re-asserted runtime detour. In C:/Users/u03a9/Repositories/pixelateds-psobb-mods/psobb-patches/atomic/pso_widescreen-asi/pso_widescreen.c:
- kStripSites[3] = { 0x004A9C0Cu, 0x004A9D28u, 0x005474D4u } (line 3598) — the SAME three sites Ephinea hooks.
- rb_deanchor_shim / rb_deanchor_after (naked, lines 3616-3642) — the wrapper: saves [0x00ACC0E8]/[0x00ACC0EC] into g_deanchor_save_x/y, writes 0x3F800000 (1.0f) to both, SWAPS the caller return for rb_deanchor_after, JMPs into the real 0x0082B158 (args untouched), then on return restores both affine floats and JMPs to the real caller return. This is the exact save/pin(1.0f)/restore semantics of FUN_52db2240. (Difference of MECHANISM only: ours is a return-address-swap tail-jmp wrapper vs Ephinea's direct CALL into a function that re-CALLs 0x82B158 — same observable effect; ours additionally has __try/__except install guards. Ours does NOT replicate the p2!=0 guard around the inner call, because ours simply forwards into 0x0082B158 unconditionally — see openQuestions.)
- INSTALL/RE-ASSERT: reassert_ingame_hooks() (lines 6086-6109) re-installs the 3 redirects every in-game worker tick; it is value-guarded/idempotent (skips a site unless it is currently stock `call 0x0082B158`, opcode==0xE8 checked, SEH-wrapped) so steady-state is quiet. deanchor_mask() (6113) reports current install state for the heartbeat. We re-assert every tick because the FE->IG scene transition WIPES code-flow .text detours back to stock (documented at lines 3594-3597, 6081-6085) — this is the in-game-leak fix path. The pin value 1.0f is HudScale-invariant, matching Ephinea.
- Our static PSOBB.IO image leaves these 3 sites STOCK (delta CSV IoLive blank); we patch only at runtime from the ASI.

**Port action:** MATCH — already ported faithfully as rb_deanchor_shim + reassert_ingame_hooks in pso_widescreen.c (same 3 sites, same save/pin-1.0f/restore around 0x0082B158). No correctness work required. Optional exactness: add the arg2!=0 inner-call guard to byte-match FUN_52db2240, and keep the per-frame value-guarded re-assert (required because our FE->IG transition wipes code-flow detours; it already covers config-reload/Device-Reset implicitly).

**Open:** EXACT VALUE OF _DAT_53175e64 is 1.0f by triangulation (PATCHES_OVERVIEW + clean port + usage + our own 0x3F800000), but NOT directly confirmed by a raw dword read of the unpacked dll — the .banana packer relocated the .rdata constant pool so the supplied linear rebase formula lands on garbage; a relocation map (or a live read, which is disallowed here) would be needed for a byte-exact confirmation. Confidence HIGH the value is 1.0f. | The p2!=0 (arg2) guard in FUN_52db2240 gates only the INNER call to 0x0082B158, not the affine pin/restore. Our rb_deanchor_shim forwards unconditionally and does not replicate this guard. Whether arg2 is ever 0 at the 3 sites in practice (and whether 0x0082B158 needs protection from a 0 arg) is unverified statically — would need a live breakpoint at the sites. | Semantic identity of the 3 hook sites: 0x004A9C0C and 0x004A9D28 sit in the 0x004A9xxx collision/raycast/positioning region (per stock-source comments: FUN_004a9174 collision-init, FUN_004a9de0 raycast, etc.), 0x005474D4 in a separate effect region. Our source labels them 'screen-space combat effects (photon blasts/billboards)'; the exact effect each draws is inferred, not symbol-confirmed. Trinity resolution.h does NOT name 0xACC0E8/EC or these 3 VAs (it is a static-anchor-table patch and does not cover this per-draw affine-pin detour) — so no Trinity cross-name is available for this function; this is purely a per-draw deanchor with no static-anchor-table equivalent. | Whether Ephinea re-runs FUN_52db2240's INSTALL on D3D Device Reset is unconfirmed (Phase-1 found no Reset hook). Moot for us since we re-assert every in-game tick, but it leaves the Ephinea-side reset behavior an open RE question.
